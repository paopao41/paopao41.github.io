<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Bloom_filter</title>
    <url>/Bloom-filter.html</url>
    <content><![CDATA[<h1 id="布隆过滤器介绍"><a href="#布隆过滤器介绍" class="headerlink" title="布隆过滤器介绍"></a>布隆过滤器介绍</h1><p><strong>基本概念：</strong>它实质上是一个很长的二进制向量和一系列随机映射函数 (Hash函数)。</p>
<p><strong>作用：</strong>它是一个空间效率高的概率型数据结构，用来告诉你:<strong>一个元素一定不存在或者可能存在</strong>。</p>
<p><strong>优点：</strong></p>
<ul>
<li><pre><code>在存储空间和时间都是常数，即hash函数的个数
</code></pre>
</li>
<li><pre><code>Hash 函数相互之间没有关系，方便由硬件并行实现。
</code></pre>
</li>
<li><pre><code>布隆过滤器不需要存储元素本身，在某些对保密要求非常严格的场合有优势。
</code></pre>
</li>
<li><pre><code>布隆过滤器可以表示全集，其它任何数据结构都不能。
</code></pre>
</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>有误判率存在</li>
<li>不支持删除</li>
</ul>
<p><strong>适用场景：</strong></p>
<ul>
<li>预防缓存穿透：布隆过滤器快速判断数据是否存在，避免通过查询数据库来判断数据是否存在。</li>
<li>网络爬虫：布隆过滤器可以用来去重已经爬取过的URL。</li>
<li>邮箱的垃圾邮件过滤。</li>
<li>黑白名单。</li>
</ul>
<h1 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h1><h4 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h4><p>布隆过滤器实现原理就是<strong>一个超大位数的数组（BitMap）和多个不同Hash算法函数</strong>。与寻常数组不同的是，BitMap一个数组元素占一个bit，这一特性决定了BitMap能够极大地<strong>节省空间</strong>。</p>
<p><img src="/../image/image-20240509090000811.png" alt="image-20240509090000811"></p>
<h4 id="添加元素"><a href="#添加元素" class="headerlink" title="添加元素"></a>添加元素</h4><p>将要添加的元素分别通过k个哈希函数计算得到k个哈希值，这k个hash值对应位数组上的k个位置，然后将这k个位置设置为1。</p>
<p><img src="/../image/image-20240509090026138.png" alt="image-20240509090026138"></p>
<p>当不同元素在计算到相同的值后，依旧保持这一位为1即可。</p>
<p><img src="/../image/image-20240509084931019.png" alt="image-20240509084931019"></p>
<h4 id="查询元素"><a href="#查询元素" class="headerlink" title="查询元素"></a>查询元素</h4><p>将要查询的元素分别通过k个哈希函数计算得到k个哈希值，这k个hash值对应位数组上的k个位置。如果这k个位置中有一个位置为0，则此元素一定不存在集合中。如果这k个位置全部为1，则这个元素可能存在。</p>
<h4 id="误判"><a href="#误判" class="headerlink" title="误判"></a>误判</h4><p>需要注意的是，布隆过滤器<strong>无法确定元素存在</strong>，只能确定元素不存在。出现的原因是多个输入经过哈希之后在相同的bit位置1了，这样就无法判断究竟是哪个输入产生的，因此误判的根源在于相同的 bit 位被多次映射且置 1。</p>
<p>这种情况也造成了布隆过滤器的<strong>删除</strong>问题，即布隆过滤器<strong>不存在删除</strong>操作。因为布隆过滤器的每一个 bit 并不是独占的，很有可能多个元素共享了某一位。如果我们直接删除这一位的话，会影响其他的元素。</p>
<h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><p><strong>防止缓存穿透</strong>。缓存穿透是指缓存和数据库中都没有的数据，而用户不断发起请求，如发起为id为“-1”的数据或id为特别大不存在的数据。这时的用户很可能是攻击者，攻击会导致数据库压力过大。使用布隆过滤器能够避免频繁查询不存在的数据，减轻数据库的压力。</p>
<p><strong>业务场景中判断用户是否阅读过某视频或文章</strong>，比如抖音或头条，当然会导致一定的误判，但不会让用户看到重复的内容。</p>
<p><strong>gun hash表</strong>， 主要是利用 <code>Bloom Filter</code>, 在常量时间内判断, 字符是否存在, 以及对应 <code>.dynsym</code> 的位置. 使用 <code>gcc -g -o hello -Wl,--hash-style=sysv(gnu) hello.c</code> 可以产生旧版本的 <code>hash</code> 表.</p>
]]></content>
      <categories>
        <category>misc</category>
      </categories>
  </entry>
  <entry>
    <title>Category_of_IP_address_ABCDE</title>
    <url>/Category-of-IP-address-ABCDE.html</url>
    <content><![CDATA[<h5 id="IP地址类型"><a href="#IP地址类型" class="headerlink" title="IP地址类型"></a>IP地址类型</h5><p>每个IP地址包括两个标识ID，网络ID和主机ID，根据网络ID的不同分为A,B,C,D,E,类地址。</p>
<p>其中A类、B类、和C类这三类地址用于TCP&#x2F;IP节点，其它两类D类和E类被用于特殊用途。</p>
<p>A、B、C三类IP地址的特征：当将IP地址写成二进制形式时，A类地址的第一位总是O，B类地址的前两位总是10，C类地址的前三位总是110。</p>
<h5 id="A类"><a href="#A类" class="headerlink" title="A类"></a>A类</h5><p>一个A类IP地址由1字节的网络地址和3字节主机地址组成，网络地址的最高位必须是“0”， 地址范围从1.0.0.0 到126.0.0.0。可用的A类网络有126个，每个网络能容纳1亿多个主机。</p>
<p>⑴ A类地址第1字节为网络地址，其它3个字节为主机地址。</p>
<p>⑵ A类地址范围：1.0.0.1—126.255.255.254</p>
<p>⑶ A类地址中的私有地址和保留地址：</p>
<p>​	① 10.X.X.X是私有地址（所谓的私有地址就是在互联网上不使用，而被用在局域网络中的地址）</p>
<p>​	② 127.X.X.X是保留地址，用做循环测试用的</p>
<h5 id="B类"><a href="#B类" class="headerlink" title="B类"></a>B类</h5><p>一个B类IP地址由2个字节的网络地址和2个字节的主机地址组成，网络地址的最高位必须是“10”，地址范围从128.0.0.0到191.255.255.255。可用的B类网络有16382个，每个网络能容纳6万多个主机 。</p>
<p>⑴ B类地址第1字节和第2字节为网络地址，其它2个字节为主机地址。</p>
<p>⑵ B类地址范围：128.0.0.1—191.255.255.254。</p>
<p>⑶ B类地址的私有地址和保留地址</p>
<p>​	① 172.16.0.0—172.31.255.255是私有地址</p>
<p>​	② 169.254.X.X是保留地址。如果你的IP地址是自动获取IP地址，而你在网络上又没有找到可用的DHCP服务器。就会得到其中一个IP。</p>
<h5 id="C类"><a href="#C类" class="headerlink" title="C类"></a>C类</h5><p>一个C类IP地址由3字节的网络地址和1字节的主机地址组成，网络地址的最高位必须是“110”。范围从192.0.0.0到223.255.255.255。C类网络可达209万余个，每个网络能容纳254个主机。</p>
<p>⑴ C类地址第1字节、第2字节和第3个字节为网络地址，第4个个字节为主机地址。另外第1个字节的前三位固定为110。</p>
<p>⑵ C类地址范围：192.0.0.1—223.255.255.254。</p>
<p>⑶ C类地址中的私有地址：192.168.X.X是私有地址。</p>
<h5 id="D类"><a href="#D类" class="headerlink" title="D类"></a>D类</h5><p>D类IP地址第一个字节以“1110”开始，它是一个专门保留的地址。它并不指向特定的网络，目前这一类地址被用在多点广播（Multicast）中。多点广播地址用来一次寻址一组计算机，它标识共享同一协议的一组计算机。</p>
<p>⑴ D类地址不分网络地址和主机地址，它的第1个字节的前四位固定为1110。</p>
<p>⑵ D类地址范围：224.0.0.1—239.255.255.254</p>
<h5 id="E类"><a href="#E类" class="headerlink" title="E类"></a>E类</h5><p>以“11100”开始，为将来使用保留。</p>
<p>全零（“0．0．0．0”）地址对应于当前主机。全“1”的IP地址（“255．255．255．255”）是当前子网的广播地址。</p>
<p>⑴ E类地址也不分网络地址和主机地址，它的第1个字节的前五位固定为11110。</p>
<p>⑵ E类地址范围：240.0.0.1—255.255.255.254</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
  </entry>
  <entry>
    <title>Photographic_learning</title>
    <url>/Photographic-learning.html</url>
    <content><![CDATA[<h1 id="摄影参数介绍"><a href="#摄影参数介绍" class="headerlink" title="摄影参数介绍"></a>摄影参数介绍</h1><ul>
<li><p>白平衡：通过调整色温尽量保持白色为原本的白色，即平衡白色的颜色。（本质是通过调整色温来调整蓝色&#x2F;红色的互补色 ）<strong>（90%的场景可以使用自动白平衡）</strong></p>
<ul>
<li>白色优先&#x2F;氛围优先</li>
<li>环境中光源很多的时候，自动白平衡可能效果不好，手动不知道设置什么的时候，设置一下白卡校准。延时拍摄需要手动设置，不然会收到忽然出现物体的影响。RAW格式后期可以无损更改白平衡</li>
</ul>
</li>
<li><p>色温：颜色的温度，色温越高 颜色越偏蓝，色温约低，颜色越偏红。  </p>
</li>
<li><p>色相：后期修改偏色，颜色发绿等等。</p>
</li>
<li><p>对焦：主要依靠镜头来完成。镜头外圈单位为m，内圈单位为inch，AF自动对焦，MF手动对焦 （当环境较暗，色彩很多的时候，自动对焦可能会不准。镜头有最小对焦距离，不得小于此，否则会无法对焦。）</p>
</li>
<li><p>AF-S:单次对焦，移动相机对焦距离也不会改变。</p>
</li>
<li><p>AF-A:自动检测物体是否移动。</p>
</li>
<li><p><strong>AF-C:连续对焦模式。（80%）</strong></p>
</li>
<li><p>DMF:自动对焦后，可手动精细对焦调节</p>
</li>
<li><p>广域对焦：屏幕范围内任何都可以对焦</p>
</li>
<li><p>点对焦SML,小中大。自由选择对焦点。</p>
</li>
<li><p>扩展点：点无法对焦的时候在附近找扩展点进行对焦</p>
</li>
<li><p>跟踪对焦：选择对焦区域。对焦点会一直跟着指定物体。</p>
</li>
<li><p>AF过渡速度：从一个对焦点移动到另一个对焦点的速度</p>
</li>
<li><p>AF转移灵敏度：如果焦点前有物体掠过的时候，是否会快速对焦到物体身上。</p>
</li>
<li><p>测光模式</p>
<ul>
<li><strong>全局测光：分割画面，单独测光，提高焦点附近权重，计算均值。（90%）</strong></li>
<li>中心测光：对画面中央物体进行测光，比如拍人的时候。</li>
<li>点测光;顾名思义。</li>
</ul>
</li>
<li><p>曝光补偿：白+黑-，<strong>拍摄人物可以适当增加一点曝光补偿</strong>。因为人体反射率25%左右。相机自动曝光会拉低曝光值，导致人物偏暗。打开直方图也可以。</p>
</li>
<li></li>
<li><p>光圈：F值。数字越大，光圈越小。</p>
<ul>
<li>光圈越大，进光亮越多。</li>
<li>光圈越大，虚化越明显，景深越浅。拍摄人物的时候放大光圈，拍摄风景的时候缩小光圈。</li>
</ul>
</li>
<li><p>快门：单位：s 1&#x2F;100秒。会计算0.01s的进光量到传感器。</p>
<ul>
<li>快门速度越慢，进光量越多。</li>
<li>快门速度还会影响拖影。 运动速度较快的设备使用较快的快门速度。轨迹则用慢速快门。</li>
<li>一般可以设置快门速度值比焦段数值更高。</li>
<li>果冻效应：物体运动过快导致每一行图像数据有时间差。导致图片会倾斜。用全域快门来解决。因为全域快门一次读取的是所有的数据。</li>
<li>电子快门，不用机械快门。但是画质会减少，因为cpu还要处理电子快门逻辑。</li>
<li>电子前lian快门。</li>
<li><strong>拍摄视频的时候的快门。和帧数相关。快门速度越快，每一帧记录物体移动时间越少。动态模糊越少。</strong></li>
<li>设置相机的快门速度一般为帧数的2倍数。</li>
</ul>
</li>
<li><p>ISO：ISO越高，画面越亮，噪点越多。增加增益，通电量。<strong>一般情况通过光圈和快门来控制曝光，实在没办法了，才会通过ISO提高。</strong>增加ISO的时候信噪比会提高。会放大本底噪声。主要是由于进光量不足，被迫只能放大信号，导致噪点被放大变得可见了。</p>
</li>
</ul>
<h1 id="挡位介绍"><a href="#挡位介绍" class="headerlink" title="挡位介绍"></a>挡位介绍</h1><ul>
<li>auto：全自动</li>
<li>P：程序自动曝光+手动曝光补偿</li>
<li>A：光圈优先，相机自动调整快门和ISO。 拍摄人像的时候，大光圈优先</li>
<li>S：快门优先挡位，相机自动调整光圈ISO。拍摄运动物体的时候。</li>
<li>C1:C2 C3，自定义挡位。</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>X86平台简称</title>
    <url>/X86%E5%B9%B3%E5%8F%B0%E7%AE%80%E7%A7%B0.html</url>
    <content><![CDATA[<h6 id="PCH"><a href="#PCH" class="headerlink" title="PCH"></a>PCH</h6><p>Platform Controller Hub，是intel公司的集成南桥，具有ICH（负责连接PCI总线，是Intel南桥芯片系列名称）的全部功能，又具有原来MCH（内存控制中心，负责连接CPU,AGP总线和内存。相当于北桥芯片）。</p>
<p>从Intel5系列芯片组开始，已经看不到北桥芯片的踪影，几乎均和CPU集成。只剩下PCH的芯片用来支持外设。而PCH芯片部分虽然比与原来的南桥芯片功能上更为丰富，但性质大体相同，与CPU之间不需要交换太多数据，因此连接总线采用DMI技术。</p>
<p><img src="/../image/image-20240229175828436.png" alt="image-20240229175828436"></p>
<center>PCH内部框图</center>

<h6 id="SMBUS"><a href="#SMBUS" class="headerlink" title="SMBUS"></a>SMBUS</h6><p>PCH提供系统管理总线SMBus主机控制器以及SMBus接口，主机控制器为处理器提供机制以启动与SMBus外围设备的通信。</p>
<h6 id="DMI"><a href="#DMI" class="headerlink" title="DMI"></a>DMI</h6><p>直接媒体接口，是CPU和PCH之间芯片到芯片的连接。DMI2.0单通道单向传输速率到达5GT&#x2F;s。</p>
<p><img src="/../image/image-20240229175620009.png" alt="image-20240229175620009"></p>
<center>图 典型的系统基于Intel®Core™i7处理器</center>]]></content>
      <categories>
        <category>server</category>
      </categories>
  </entry>
  <entry>
    <title>bakeup_region</title>
    <url>/bakeup-region.html</url>
    <content><![CDATA[<h1 id="备份区域以及RTC介绍"><a href="#备份区域以及RTC介绍" class="headerlink" title="备份区域以及RTC介绍"></a>备份区域以及RTC介绍</h1>]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>cache_arm_x86</title>
    <url>/cache-arm-x86.html</url>
    <content><![CDATA[<h1 id="ARM架构"><a href="#ARM架构" class="headerlink" title="ARM架构"></a>ARM架构</h1><h5 id="cache多级相连"><a href="#cache多级相连" class="headerlink" title="cache多级相连"></a>cache多级相连</h5><p>cache是多级的，在一个系统中会有多级cache。</p>
<p><img src="/../image/image-20240910090615943.png" alt="image-20240910090615943"></p>
<p>一般来说，在bit-little中，L1在core中，且L1缓存又分为 I-cache指令环境和 D-cache数据缓存。L2 cache在cluster中，L3则在BUS总线上。</p>
<p>当CPU计算时，首先去L1去寻找需要的数据，如果没有则去L2寻找，接着从L3中寻找，如果都没有，则从内存中读取数据。所以，如果某些数据需要经常被访问，那么这些数据存放在L1中的效率会最高。</p>
<h5 id="计算机缓存行cache-line"><a href="#计算机缓存行cache-line" class="headerlink" title="计算机缓存行cache line"></a>计算机缓存行cache line</h5><p>高速缓存其实就是一组称之为缓存行cache line的固定大小数据块。其大小是以突发写或者突发读的周期大小为基础的。</p>
<p>即使处理器只存取一个字节的存储器，高速缓存控制器也启动整个存取器访问周期并请求整个数据块。缓存行第一个字节的地址总是突发周期尺寸的倍数。缓存行的起始位置总是与突发周期的开头保持一致。</p>
<p>当从内存中取单元到cache中时，会一次取一个cacheline大小的内存区域到cache中，然后存进相应的cacheline中。</p>
<h5 id="cache一般和MMU结合使用"><a href="#cache一般和MMU结合使用" class="headerlink" title="cache一般和MMU结合使用"></a>cache一般和MMU结合使用</h5><p>很多时候cache都是和MMU一起使用的（即同时开启或关闭）。因为MMU的页表entry属性中控制着内存权限和cache缓存策略等等。</p>
<p><img src="/../image/image-20240910091751607.png" alt="image-20240910091751607"></p>
<p>在ARM架构中，L1 cache都是VIPT（virtual index physical tag 虚拟地址做索引，物理地址做tag）的，也就是当有一个虚拟地址送进来，MMU在开始进行地址翻译的时候，Virtual Index就可以去L1 cache中查询了，MMU查询和L1 cache的index查询是同时进行的。如果L1 Miss了，则再去查询L2，L2还找不到则再去查询L3。 注意在arm架构中，仅仅L1是VIPT，L2和L3都是PIPT。</p>
<p><img src="/../image/image-20240910094036583.png" alt="image-20240910094036583"></p>
]]></content>
  </entry>
  <entry>
    <title>cma_mma</title>
    <url>/cma-mma.html</url>
    <content><![CDATA[<h1 id="cma以及mma内存分配介绍"><a href="#cma以及mma内存分配介绍" class="headerlink" title="cma以及mma内存分配介绍"></a>cma以及mma内存分配介绍</h1><p>mstar平台为例子。</p>
<h5 id="问题引入："><a href="#问题引入：" class="headerlink" title="问题引入："></a>问题引入：</h5><p>ssr931g在插入usb过程中会报错。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cma:cma_calloc:memory range at ptrval is busy,  retrying。</span><br></pre></td></tr></table></figure>

<h5 id="cma介绍"><a href="#cma介绍" class="headerlink" title="cma介绍"></a>cma介绍</h5><p>CMA –&gt; 连续内存分配器。是一种用于申请大量的，并且物理上连续的内存块的方法。在设备驱动USB,HOST,DMA,ETH PHY中起关键作用。</p>
<h5 id="内存分配-–-Sstar"><a href="#内存分配-–-Sstar" class="headerlink" title="内存分配 –&gt; Sstar"></a>内存分配 –&gt; Sstar</h5><p><img src="/../image/image-20240913102052749.png" alt="image-20240913102052749"></p>
<p>sstar的内存分配图。如上。LX_MEM有可能有好几块，例如某些SOC上会有双通道DDR，每个DDR上面会各自分配一块LX_MEM。还有某些特别的情况，一颗DDR上面可能会分配多个LX_MEM。多个LX_MEM的命名规则为LX_MEM1、LX_MEM2，以此类推。</p>
<p>注意：MMA Heap以及HW IP Layout分配出来的内存在物理上是连续的，但是LX_MEM分配给linux kernel的不一定是在物理上连续的。</p>
]]></content>
  </entry>
  <entry>
    <title>create_sys</title>
    <url>/create-sys.html</url>
    <content><![CDATA[<h1 id="linux设备驱动注册sys文件访问"><a href="#linux设备驱动注册sys文件访问" class="headerlink" title="linux设备驱动注册sys文件访问"></a>linux设备驱动注册sys文件访问</h1><p><strong>device_create_file</strong> 在当前设备的sys目录下创建一个属性对应的文件。</p>
<h4 id="设备属性文件"><a href="#设备属性文件" class="headerlink" title="设备属性文件"></a>设备属性文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct device_attribute &#123;</span><br><span class="line">    struct attribute        attr;</span><br><span class="line">    ssize_t (*show)(struct device *dev, struct device_attribute *attr,</span><br><span class="line">            char *buf);</span><br><span class="line">    ssize_t (*store)(struct device *dev, struct device_attribute *attr,</span><br><span class="line">            const char *buf, size_t count);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">#define DEVICE_ATTR(_name, _mode, _show, _store) \</span><br><span class="line">        struct device_attribute dev_attr_##_name = __ATTR(_name, _mode, _show, _store)</span><br><span class="line">        </span><br><span class="line">    定义一个device_attribute类型的变量，##代表将两边的名字拼接起来，因此，我们得到的变量名称是含有dev_attr的，该宏定义需要	传入四个参数，name，mode，show，store分别代表文件名，文件权限，show回调函数（cat 节点），store回调函数（echo 节		点），参数mode，可以用宏（S_IRUSR、S_IWUSR、S_IXUSR等等）来定义，表示读写权限。</span><br><span class="line"></span><br><span class="line">extern int device_create_file(struct device *device,</span><br><span class="line">                const struct device_attribute *entry);</span><br><span class="line">extern void device_remove_file(struct device *dev,</span><br><span class="line">                const struct device_attribute *attr);</span><br><span class="line">                </span><br><span class="line">       device表示设备，其成员中有个bus_type变量，用于指定设备挂载在某个总线上，并且会在总线的devices目录下创建一个属于该设备的目录。</span><br></pre></td></tr></table></figure>



<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#用于在sys文件系统中创建一个设备属性</span><br><span class="line"></span><br><span class="line">#include &lt;linux/module.h&gt;</span><br><span class="line">#include &lt;linux/device.h&gt;</span><br><span class="line">#include &lt;linux/kernel.h&gt;</span><br><span class="line"></span><br><span class="line">static ssize_t my_sysfs_show(struct device *dev, struct device_attribute *attr, char *buf) &#123;</span><br><span class="line">    return sprintf(buf, &quot;Hello, world!\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static ssize_t my_sysfs_store(struct device *dev, struct device_attribute *attr, const char *buf, size_t count) &#123;</span><br><span class="line">    // 处理写入的数据</span><br><span class="line">    return count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static DEVICE_ATTR(my_sysfs_file, 0664, my_sysfs_show, my_sysfs_store);</span><br><span class="line"></span><br><span class="line">static int __init my_driver_init(void) &#123;</span><br><span class="line">    struct device *dev = /* 获取设备指针 */;</span><br><span class="line">    int retval;</span><br><span class="line"></span><br><span class="line">    retval = device_create_file(dev, &amp;dev_attr_my_sysfs_file);</span><br><span class="line">    if (retval) &#123;</span><br><span class="line">        printk(KERN_ERR &quot;Failed to create sysfs file\n&quot;);</span><br><span class="line">        return retval;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    printk(KERN_INFO &quot;Sysfs file created\n&quot;);</span><br><span class="line">    return 0;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">static void __exit my_driver_exit(void) &#123;</span><br><span class="line">    struct device *dev = /* 获取设备指针 */;</span><br><span class="line">    device_remove_file(dev, &amp;dev_attr_my_sysfs_file);</span><br><span class="line">    printk(KERN_INFO &quot;Sysfs file removed\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module_init(my_driver_init);</span><br><span class="line">module_exit(my_driver_exit);</span><br><span class="line"></span><br><span class="line">MODULE_LICENSE(&quot;GPL&quot;);</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>device_tree</title>
    <url>/device-tree.html</url>
    <content><![CDATA[<h1 id="设备树"><a href="#设备树" class="headerlink" title="设备树"></a>设备树</h1>]]></content>
  </entry>
  <entry>
    <title>emmc_partation</title>
    <url>/emmc-partation.html</url>
    <content><![CDATA[<h1 id="emmc分区"><a href="#emmc分区" class="headerlink" title="emmc分区"></a>emmc分区</h1><p>emmc有默认四个物理分区。是出场即默认存在的。</p>
<p><img src="/../image/image-20240807190154285.png" alt="image-20240807190154285"></p>
<p>各个分区有独立的地址，都是从0x00开始的。Boot1，Boot2和RPMB的大小会在出厂的时候就设定好。两个boot的大小是完全一致的。由Extended CSD register的BOOT_SIZE_MULT Filed决定。大小的计算公式如下：</p>
<p><img src="/../image/image-20240807191047225.png" alt="image-20240807191047225"></p>
<p>一般情况下，Boot Area Partition 的大小都为 4 MB，即 BOOT_SIZE_MULT 为 32，部分芯片厂家会提供改写 BOOT_SIZE_MULT 的功能来改变 Boot Area Partition 的容量大小。BOOT_SIZE_MULT 最大可以为 255，即 Boot Area Partition 的最大容量大小可以为 255 x 128 KB &#x3D; 32640 KB &#x3D; 31.875 MB。</p>
<h2 id="分区编制"><a href="#分区编制" class="headerlink" title="分区编制"></a>分区编制</h2><p>具体的数据读写访问操作实际访问哪一个硬件分区。是由EMMC 的 Extended CSC register的PARTITION_CONFIG Field 中 的 Bit[2:0]: PARTITION_ACCESS 决定的，用户可以通过配置来切换硬件分区的访问。</p>
<p>也就是说，用户在访问特定的分区前，需要先发送命令，配置 PARTITION_ACCESS，然后再发送相关的数据访问请求。</p>
<h2 id="从Boot-area启动"><a href="#从Boot-area启动" class="headerlink" title="从Boot area启动"></a>从Boot area启动</h2><p>eMMC 中定义了 Boot State，在 Power-up、HW reset 或者 SW reset 后，如果满足一定的条件，eMMC 就会进入该 State。进入 Boot State 的条件如下：</p>
<p><strong>Original Boot Operation</strong><br>CMD 信号保持低电平不少于 74 个时钟周期，会触发 Original Boot Operation，进入 Boot State。</p>
<p><strong>Alternative Boot Operation</strong><br>在 74 个时钟周期后，在 CMD 信号首次拉低或者 Host 发送 CMD1 之前，Host 发送参数为 0xFFFFFFFA 的 COM0时，会触发 Alternative Boot Operation，进入 Boot State。</p>
<p>在 Boot State 下，如果有配置 BOOT_ACK，eMMC 会先发送 “010” 的 ACK 包，接着 eMMC 会将最大为 128Kbytes x BOOT_SIZE_MULT 的 Boot Data 发送给 Host。传输过程中，Host 可以通过拉高 CMD 信号 (Original Boot 中)，或者发送 Reset 命令 (Alternative Boot 中) 来中断 eMMC 的数据发送，完成 Boot Data 传输。</p>
<p>Boot Data 根据 Extended CSD register 的 PARTITION_CONFIG Field 的 Bit[5:3]:BOOT_PARTITION_ENABLE 的设定，可以从 Boot Area Partition 1、Boot Area Partition 2 或者 User Data Area 读出。</p>
<blockquote>
<p>Boot Data 存储在 Boot Area 比在 User Data Area 中要更加的安全，可以减少意外修改导致系统无法启动，同时无法更新系统的情况出现。</p>
</blockquote>
<p><a href="http://www.wowotech.net/basic_tech/emmc_partitions.html">http://www.wowotech.net/basic_tech/emmc_partitions.html</a></p>
<h2 id="RPMB-Partition"><a href="#RPMB-Partition" class="headerlink" title="RPMB Partition"></a>RPMB Partition</h2><p>Replay Protected Memory Block Partition是emmc中一个具有安全特性的分区。eMMC 在写入数据到 RPMB 时，会校验数据的合法性，只有指定的 Host 才能够写入，同时在读数据时，也提供了签名机制，保证 Host 读取到的数据是 RPMB 内部数据，而不是攻击者伪造的数据。</p>
<p>RPMB 在实际应用中，通常用于存储一些有防止非法篡改需求的数据，例如手机上指纹支付相关的公钥、序列号等。RPMB 可以对写入操作进行鉴权，但是读取并不需要鉴权，任何人都可以进行读取的操作，因此存储到 RPMB 的数据通常会进行加密后再存储。</p>
<h4 id="容量大小"><a href="#容量大小" class="headerlink" title="容量大小"></a>容量大小</h4><p>两个 RPMB Partition 的大小是由 Extended CSD register 的 BOOT_SIZE_MULT Field 决定，大小的计算公式如下：</p>
<p><strong>Size &#x3D; 128Kbytes x BOOT_SIZE_MULT</strong></p>
<p>一般情况下，Boot Area Partition 的大小为 4 MB，即 RPMB_SIZE_MULT 为 32，部分芯片厂家会提供改写 RPMB_SIZE_MULT 的功能来改变 RPMB Partition 的容量大小。RPMB_SIZE_MULT 最大可以为 128，即 Boot Area Partition 的最大容量大小可以为 128 x 128 KB &#x3D; 16384 KB &#x3D; 16 MB。</p>
<h4 id="Replay-Protected-原理"><a href="#Replay-Protected-原理" class="headerlink" title="Replay Protected 原理"></a>Replay Protected 原理</h4><p>使用 eMMC 的产品，在产线生产时，会为每一个产品生产一个唯一的 256 bits 的 Secure Key，烧写到 eMMC 的 OTP 区域（只能烧写一次的区域），同时 Host 在安全区域中（例如：TEE）也会保留该 Secure Key。</p>
<p>在 eMMC 内部，还有一个RPMB Write Counter。RPMB 每进行一次合法的写入操作时，Write Counter 就会自动加一 。</p>
<p>通过 Secure Key 和 Write Counter 的应用，RMPB 可以实现数据读取和写入的 Replay Protect。</p>
<h2 id="User-Data-Area"><a href="#User-Data-Area" class="headerlink" title="User Data Area"></a>User Data Area</h2><p>通常是EMMC分区中最大的一个。在实际产品中共，是主要区域。</p>
<h4 id="容量大小-1"><a href="#容量大小-1" class="headerlink" title="容量大小"></a>容量大小</h4><p>容量大小不需要设置，在配置完其他区域之后，扣除Enhanced attribute损耗的容量，剩余的就是UDA的容量。</p>
<h4 id="区域属性"><a href="#区域属性" class="headerlink" title="区域属性"></a>区域属性</h4><p>eMMC 标准中，支持为 UDA 中一个特定大小的区域设定 Enhanced attribute。与 GPP 中的 Enhanced attribute 相同，eMMC 标准也没有定义该区域设定 Enhanced attribute 后对 eMMC 的影响。Enhanced attribute 的具体作用，由芯片制造商定义、</p>
<ul>
<li>Default, 未设定 Enhanced attribute。</li>
<li>Enhanced storage media， 设定该区域为 Enhanced storage media。</li>
</ul>
<p>在实际的产品中，UDA 区域设定为 Enhanced storage media 后，一般是把该区域的存储介质从 MLC 改变为 SLC。通常，产品中可以将某一个 SW Partition 设定为 Enhanced storage media，以获得更好的性能和健壮性。</p>
<h2 id="命令说明"><a href="#命令说明" class="headerlink" title="命令说明"></a>命令说明</h2><h5 id="mmc-dev-dev-part"><a href="#mmc-dev-dev-part" class="headerlink" title="mmc dev [dev] [part]"></a>mmc dev [dev] [part]</h5><p>切换物理分区。mmc 设备编号，第一个为 0 part： 0 表示不访问引导分区 1 表示访问引导分区 1（boot0） 2 表示访问引导分区 2（boot1）</p>
<p>eg：切换到引导分区 1  –&gt;         mmc dev 0 1</p>
<h5 id="mmc-bootbus-dev-boot-bus-width-reset-boot-bus-width-boot-mode"><a href="#mmc-bootbus-dev-boot-bus-width-reset-boot-bus-width-boot-mode" class="headerlink" title="mmc bootbus [dev] [boot_bus_width] [reset_boot_bus_width] [boot_mode]"></a>mmc bootbus [dev] [boot_bus_width] [reset_boot_bus_width] [boot_mode]</h5><p>设置总线位宽。</p>
<h5 id="mmc-partconf-dev-boot-ack-boot-partition-partition-access"><a href="#mmc-partconf-dev-boot-ack-boot-partition-partition-access" class="headerlink" title="mmc partconf  [dev ] [boot_ack ] [boot_partition ] [partition_access]"></a>mmc partconf  [dev ] [boot_ack ] [boot_partition ] [partition_access]</h5><p>设置启动分区，dev 为mmc设备编号。boot_ack为是否应答。boot_partition用户选择发送到主机的引导数据。partition_access用户选择要访问的分区。</p>
]]></content>
  </entry>
  <entry>
    <title>env</title>
    <url>/env.html</url>
    <content><![CDATA[<h1 id="env小记"><a href="#env小记" class="headerlink" title="env小记"></a>env小记</h1>]]></content>
  </entry>
  <entry>
    <title>file_operation_poll</title>
    <url>/file-operation-poll.html</url>
    <content><![CDATA[<h1 id="poll机制-使用方法"><a href="#poll机制-使用方法" class="headerlink" title="poll机制 使用方法"></a>poll机制 使用方法</h1><p>使用非阻塞IO的应用程序通常会使用select和poll系统调用查询是否可对设备进行无阻塞的访问，这两个系统调用最终会引发设备驱动中poll函数被执行。</p>
<h6 id="atomic-t介绍"><a href="#atomic-t介绍" class="headerlink" title="atomic_t介绍"></a>atomic_t介绍</h6><p><strong><code>atomic_t</code></strong>: 这是一个用于定义原子变量的类型。原子变量是一种特殊的变量，可以在多线程环境下安全地进行操作，而不需要使用锁来防止竞争条件。<code>atomic_t</code> 通常用于存储整数值，并且提供了一组原子操作函数来保证操作的原子性。</p>
<p><strong><code>ATOMIC_INIT(0)</code></strong>: 这是一个宏，用于初始化原子变量。在这种情况下，<code>ATOMIC_INIT(0)</code> 将 <code>gstSub1gMsgFlag</code> 初始化为 0。原子变量的初始化通常需要使用这个宏，而不是直接赋值，因为这样可以保证初始化过程的线程安全性。</p>
<h6 id="DECLARE-WAIT-QUEUE-HEAD介绍"><a href="#DECLARE-WAIT-QUEUE-HEAD介绍" class="headerlink" title="DECLARE_WAIT_QUEUE_HEAD介绍"></a>DECLARE_WAIT_QUEUE_HEAD介绍</h6><p>用于在linux内核编程中声明和初始化等待队列（wait queue）头的宏。等待队列用于在某些条件满足之前让线程处于睡眠状态，并且在条件满足的时候唤醒他们。常用于<strong>阻塞式等待</strong>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DECLARE_WAIT_QUEUE_HEAD(msg_wait)</span><br><span class="line">等价于</span><br><span class="line">wait_queue_head_t msg_wait;</span><br><span class="line">init_waitqueue_head(&amp;msg_wait);</span><br></pre></td></tr></table></figure>

<h6 id="wake-up-interruptible介绍"><a href="#wake-up-interruptible介绍" class="headerlink" title="wake_up_interruptible介绍"></a>wake_up_interruptible介绍</h6><p>用于唤醒等待队列中处于可中断睡眠状态的进程。进程被唤醒后，会从睡眠状态恢复并执行对应任务（.poll绑定函数）。</p>
<h6 id="poll-wait-file-msg-wait-wait-介绍"><a href="#poll-wait-file-msg-wait-wait-介绍" class="headerlink" title="poll_wait(file, &amp;msg_wait, wait);介绍"></a>poll_wait(file, &amp;msg_wait, wait);介绍</h6><p>将当前进程mgs_wait添加到指定的等待队列中。</p>
<p>poll函数返回事件的状态。</p>
<p><img src="/../image/image-20240812150319868.png" alt="image-20240812150319868"></p>
]]></content>
  </entry>
  <entry>
    <title>gdb</title>
    <url>/gdb.html</url>
    <content><![CDATA[<h1 id="gcc编译过程"><a href="#gcc编译过程" class="headerlink" title="gcc编译过程"></a>gcc编译过程</h1><ul>
<li>预处理阶段，完成宏定义和include文件展开工作 (.i）</li>
<li>根据编译参数进行不同程度的优化，编译成汇编代码 (.S)</li>
<li>用汇编器把汇编代码进一步生成目标代码 (.o)</li>
<li>用连接器把生成目标的代码和系统或者用户提供的库连接起来，生成可执行文件</li>
</ul>
<h1 id="gdb功能"><a href="#gdb功能" class="headerlink" title="gdb功能"></a>gdb功能</h1><ul>
<li><p>设置断点</p>
</li>
<li><p>监视程序变量的值</p>
</li>
<li><p>程序单步执行</p>
</li>
<li><p>显示修改变量值</p>
</li>
<li><p>显示修改寄存器</p>
</li>
<li><p>查看程序堆栈情况</p>
</li>
</ul>
<h1 id="gdb调试使用方式"><a href="#gdb调试使用方式" class="headerlink" title="gdb调试使用方式"></a>gdb调试使用方式</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gcc -g  main.c                      //在目标文件加入源代码的信息</span><br><span class="line">gdb a.out       </span><br><span class="line"> </span><br><span class="line">(gdb) start                         //开始调试</span><br><span class="line">(gdb) n                             //一条一条执行</span><br><span class="line">(gdb) step/s                        //执行下一条，如果函数进入函数</span><br><span class="line">(gdb) backtrace/bt                  //查看函数调用栈帧</span><br><span class="line">(gdb) info/i locals                 //查看当前栈帧局部变量</span><br><span class="line">(gdb) frame/f                       //选择栈帧，再查看局部变量</span><br><span class="line">(gdb) print/p                       //打印变量的值</span><br><span class="line">(gdb) finish                        //运行到当前函数返回</span><br><span class="line">(gdb) set var sum=0                 //修改变量值</span><br><span class="line">(gdb) list/l 行号或函数名             //列出源码</span><br><span class="line">(gdb) display/undisplay sum         //每次停下显示变量的值/取消跟踪</span><br><span class="line">(gdb) break/b  行号或函数名           //设置断点</span><br><span class="line">(gdb) continue/c                    //连续运行</span><br><span class="line">(gdb) info/i breakpoints            //查看已经设置的断点</span><br><span class="line">(gdb) delete breakpoints 2          //删除某个断点</span><br><span class="line">(gdb) disable/enable breakpoints 3  //禁用/启用某个断点</span><br><span class="line">(gdb) break 9 if sum != 0           //满足条件才激活断点</span><br><span class="line">(gdb) run/r                         //重新从程序开头连续执行</span><br><span class="line">(gdb) watch input[4]                //设置观察点</span><br><span class="line">(gdb) info/i watchpoints            //查看设置的观察点</span><br><span class="line">(gdb) x/7b input                    //打印存储器内容，b--每个字节一组，7--7组</span><br><span class="line">(gdb) disassemble                   //反汇编当前函数或指定函数</span><br><span class="line">(gdb) si                            // 一条指令一条指令调试 而 s 是一行一行代码</span><br><span class="line">(gdb) info registers                // 显示所有寄存器的当前值</span><br><span class="line">(gdb) x/20 $esp                     //查看内存中开始的20个数</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>git_svn_opt</title>
    <url>/git-svn-opt.html</url>
    <content><![CDATA[<h2 id="git-svn常用操作"><a href="#git-svn常用操作" class="headerlink" title="git svn常用操作"></a>git svn常用操作</h2><h3 id="git"><a href="#git" class="headerlink" title="git"></a>git</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git add xxx 											// git添加文件</span><br><span class="line"></span><br><span class="line">git commit -m &quot;add syslog &amp;&amp; ipaddress&quot;					// git提交本地</span><br><span class="line"></span><br><span class="line">git push												// git提交远端</span><br></pre></td></tr></table></figure>

<h3 id="svn"><a href="#svn" class="headerlink" title="svn"></a>svn</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">svn merge -r HEAD:12098 .        // svn回退到某个版本</span><br></pre></td></tr></table></figure>



]]></content>
  </entry>
  <entry>
    <title>hisi3519AV200_debug</title>
    <url>/hisi3519AV200-debug.html</url>
    <content><![CDATA[<h4 id="hisi常用debug查看"><a href="#hisi常用debug查看" class="headerlink" title="hisi常用debug查看"></a>hisi常用debug查看</h4><ul>
<li>hiddrs查看ddr占用</li>
<li>cat &#x2F;dev&#x2F;logmpp 查看设备mpplog</li>
<li></li>
</ul>
<h4 id="媒体处理部分"><a href="#媒体处理部分" class="headerlink" title="媒体处理部分"></a>媒体处理部分</h4>]]></content>
      <categories>
        <category>dsp_hisi_misc</category>
      </categories>
  </entry>
  <entry>
    <title>initramfs</title>
    <url>/initramfs.html</url>
    <content><![CDATA[<p>在完成rootfs的构建之后，通过配置内核支持Initramfs，并设置好rootfs的路径即可编译出带有文件系统的vmliunx</p>
<h3 id="initramfs简介"><a href="#initramfs简介" class="headerlink" title="initramfs简介"></a>initramfs简介</h3><h5 id="是什么"><a href="#是什么" class="headerlink" title="是什么"></a>是什么</h5><p>initramfs 即 initram file system，翻译成中文意思就是 初始 ram 文件系统，基于 tmpfs，是一种大小灵活，直接作用在内存中的文件系统。initramfs 包含的工具和脚本，在正式的根文件系统的初始化脚本 init 启动之前，就被挂载。initramfs 是可选的，内核编译选项默认开启 initramfs（initrd）。那么什么情况下考虑使用 initramfs 呢？</p>
<ul>
<li>加载模块，如三方驱动</li>
<li>定制化启动系统</li>
<li>制作一个很小的 rescue shell</li>
<li>内核不能，但是用户态可以完成的命令</li>
</ul>
<p>initramfs 在内核启动的早期提供用一个户态环境，用于完成在内核启动阶段不易完成的工作。initramfs 包含的工具可以解密抽象层（用于加密的文件系统），逻辑卷管理器，软件 RAID，蓝牙驱动程序等。</p>
<p>一个 initramfs 至少包含一个文件，即 &#x2F;init，内核将这个文件执行起来的进程设置为 main init 进程，pid &#x3D; 1。内核挂载 initramfs 时，文件系统的根分区并没有挂载，所以无法访问文件系统中的文件。大多数嵌入式设备可能需要一个 shell，那么也会在 initramfs 打包进一个 shell。如果还需要其他工具或者脚本，也可以打包进 initramfs，但注意，必须包含依赖，因为 initramfs 是一个能够独立运行的 ram 文件系统</p>
<h5 id="如何工作"><a href="#如何工作" class="headerlink" title="如何工作"></a>如何工作</h5><p>initramfs 和我们常见的文件系统类似，可能存在 &#x2F;usr、&#x2F;bin 等目录。里面包含着我们的工具和脚本。initramfs 需要使用 cpio 来归档，cpio 是一个有着古老历史的文件归档解决方案，类似于 linux 中常用的 tar，或者 windows 中的 zip，主要作用是将多个文件打包成一个文件（但是没有压缩）。使用 cpio，是因为其代码易于实现，而且能够兼容更多的设备。</p>
<p>归档之后，需要考虑 initramfs 文件的体积，要进一步压缩，减少内存或磁盘的占用。所有文件，工具，库，配置设置（如果适用）等都放入 cpio 归档后，使用 gzip 实用程序压缩 cipo 文件，并将其与linux 内核一起存储。引导加载程序（通常是 grub 或者 uboot）将在引导时将其提供给内核，以便内核知道需要一个 initramfs。</p>
<p>内核一旦检测到 initramfs，会创建一个 tmpfs 文件系统，提取 gzip 中存档的 initramfs，并存入 tmpfs 中，内核启动 tmpfs 文件系统中的 init 脚本。该脚本用于挂载实际的根文件系统，当完成根文件系统和其他的一些重要的文件系统的安装之后，init 脚本会切换至真实的根文件系统，并在系统上调用 &#x2F; sbin &#x2F; init 二进制文件，进行后续的启动过程。</p>
<h3 id="initramfs配置"><a href="#initramfs配置" class="headerlink" title="initramfs配置"></a>initramfs配置</h3><p>使用make menuconfig进入内核菜单后。</p>
<p>配置General setup —&gt; [*]Initial RAM filesystem and RAM disk (initramfs&#x2F;initrd) support。</p>
<p>配置General setup —&gt; ()Initramfs source file(s) [注]在括号里写入构建的rootfs路径，绝对路径和相对路径均可。此处填写的是busybox编译的rootfs.cpio。</p>
<p>make后重新编译内核即可</p>
<h3 id="与ramdisk区别"><a href="#与ramdisk区别" class="headerlink" title="与ramdisk区别"></a>与ramdisk区别</h3><p>ramdisk是在一块内存区域中创建的块设备，用于存放文件系统。ramdisk的<strong>容量是固定的</strong>，不能象ramfs一样动态增长。ramdisk<strong>需要内核的文件系统驱动程序</strong>（如ext2）来操作其上的数据，而ramfs则是内核的天然特性，无需额外的驱动程序。ramdisk也象其他文件系统设备一样， 需要在块设备和内存中的磁盘高速缓存之间复制数据，而这种数据复制实际不必要的</p>
]]></content>
  </entry>
  <entry>
    <title>kdump_mmc</title>
    <url>/kdump-mmc.html</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>当内存发生panic的时候，需要把panic的内容以日志的方式记录下来。目前有几种方式，kdump，mtdoops，crashlog（openwrt特有），以及pstore。</p>
<p>kdump主要用在x86系统上，因为它使用大量的内存和硬盘信息。</p>
<p>mtdoops和crashlog主要用于嵌入式系统。记录文本日志。</p>
<h2 id="mtdoops"><a href="#mtdoops" class="headerlink" title="mtdoops"></a>mtdoops</h2><p>mtdoop功能在发生oops时,把msg区写入特定的mt分区。写入过程，不支持文件系统。<strong>直接二进制文本写</strong>。 它需要由mtd驱动的支持，就是mtd驱动支持mtd_panic_write。也就是原子式写入。不能被中断。一般flash不支持。 在mtdoops.c文件，并在标准内核的drivers&#x2F;mtd&#x2F;mtdcore中。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mtd_panic_write</span><br><span class="line">	--&gt; _panic_write</span><br><span class="line">		---&gt; mtd-&gt;_panic_write = panic_nand_write / onenand_panic_write / concat_panic_write 在具体驱动实现</span><br></pre></td></tr></table></figure>

<p>在初始化过程中，需要指定写入哪个分区，对应的分区名&#x2F;号，可以写入的size是多少。使用kmsg_dump_register注册一个cxt-&gt;dump.dump绑定的回调函数 (eg:mmcoops_do_dump)，此函数主要是读取kmsg区的内容，直接调用mtd-&gt;write的驱动进行写操作。</p>
<h2 id="crashlog"><a href="#crashlog" class="headerlink" title="crashlog"></a>crashlog</h2><p>在linux内核启动的时候，保留一块64K的内存，用于记录panic日志，crashlog发生在oop时候，把msg写入之前分配好的MEM区域，并加上magic，再重启后，check magic ok，则把上次日志放入到&#x2F;sys&#x2F;kernel&#x2F;debug&#x2F;crashlog。</p>
<h2 id="pstore"><a href="#pstore" class="headerlink" title="pstore"></a>pstore</h2><p>pstore最初是用于系统发生oops或panic时，自动保存内核log buffer中的日志。不过在当前内核版本中，其已经支持了更多的功能，如保存console日志、ftrace消息和用户空间日志。同时，它还支持将这些消息保存在不同的存储设备中，如内存、块设备或mtd设备。 为了提高灵活性和可扩展性，pstore将以上功能分别抽象为前端和后端，其中像dmesg、console等为pstore提供数据的模块称为前端，而内存设备、块设备等用于存储数据的模块称为后端，pstore core则分别为它们提供相关的注册接口。</p>
<p>通过模块化的设计，实现了前端和后端的解耦，因此若某些模块需要利用pstore保存信息，就可以方便地向pstore添加新的前端。而若需要将pstore数据保存到新的存储设备上，也可以通过向其添加后端设备的方式完成。</p>
<p><img src="/../image/image-20240924102113713.png" alt="image-20240924102113713"></p>
<p>除此之外，pstore还设计了一套pstore文件系统，用于查询和操作上一次重启时已经保存的pstore数据。当该文件系统被挂载时，保存在backend中的数据将被读取到pstore fs中，并以文件的形式显示。</p>
<p>源码在&#x2F;fs&#x2F;pstore&#x2F;ram_core.c</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fs/pstore/</span><br><span class="line">├── ftrace.c		# ftrace 前端的实现</span><br><span class="line">├── inode.c		# pstore 文件系统的注册与操作</span><br><span class="line">├── internal.h</span><br><span class="line">├── Kconfig</span><br><span class="line">├── Makefile</span><br><span class="line">├── platform.c		# pstore 前后端功能的核心</span><br><span class="line">├── pmsg.c		# pmsg 前端的实现</span><br><span class="line">├── ram.c			# pstore/ram 后端的实现,dram空间分配与管理</span><br><span class="line">├── ram_core.c		# pstore/ram 后端的实现,dram的读写操作</span><br></pre></td></tr></table></figure>

<p>oops&#x2F;panic日志位于 pstore 目录下的dmesg-ramoops-x文件中，根据缓冲区大小可以有多个文件，x从0开始。<br>函数调用序列日志位于 pstore 目录下的ftrace-ramoops文件中。</p>
<h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><h4 id="内核配置"><a href="#内核配置" class="headerlink" title="内核配置"></a>内核配置</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONFIG_PSTORE=y</span><br><span class="line">CONFIG_PSTORE_CONSOLE=y</span><br><span class="line">CONFIG_PSTORE_PMSG=y</span><br><span class="line">CONFIG_PSTORE_RAM=y</span><br><span class="line">CONFIG_PANIC_TIMEOUT=-1</span><br><span class="line"></span><br><span class="line">由于log数据存放于DDR，不能掉电，只能依靠自动重启机制来查看，故而要配置：CONFIG_PANIC_TIMEOUT，让系统在 panic 后能自动重启。</span><br><span class="line"></span><br><span class="line">mtdoops：</span><br><span class="line">CONFIG_PSTORE=y</span><br><span class="line">CONFIG_PSTORE_CONSOLE=y</span><br><span class="line">CONFIG_PSTORE_PMSG=y</span><br><span class="line">CONFIG_MTD_OOPS=y</span><br><span class="line">CONFIG_MAGIC_SYSRQ=y</span><br><span class="line"></span><br><span class="line">blkoops：</span><br><span class="line">CONFIG_PSTORE=y</span><br><span class="line">CONFIG_PSTORE_CONSOLE=y</span><br><span class="line">CONFIG_PSTORE_PMSG=y</span><br><span class="line">CONFIG_PSTORE_BLK=y</span><br><span class="line">CONFIG_MTD_PSTORE=y</span><br><span class="line">CONFIG_MAGIC_SYSRQ=y</span><br></pre></td></tr></table></figure>

<h4 id="设备树配置"><a href="#设备树配置" class="headerlink" title="设备树配置"></a>设备树配置</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ramoops_mem: ramoops_mem &#123;</span><br><span class="line">    reg = &lt;0x0 0x110000 0x0 0xf0000&gt;;</span><br><span class="line">    reg-names = &quot;ramoops_mem&quot;;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">ramoops &#123;</span><br><span class="line">    compatible = &quot;ramoops&quot;;</span><br><span class="line">    record-size = &lt;0x0 0x20000&gt;;</span><br><span class="line">    console-size = &lt;0x0 0x80000&gt;;</span><br><span class="line">    ftrace-size = &lt;0x0 0x00000&gt;;</span><br><span class="line">    pmsg-size = &lt;0x0 0x50000&gt;;</span><br><span class="line">    memory-region = &lt;&amp;ramoops_mem&gt;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="bootargs分区配置"><a href="#bootargs分区配置" class="headerlink" title="bootargs分区配置"></a>bootargs分区配置</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">方案1：</span><br><span class="line">bootargs = &quot;console=ttyS1,115200 loglevel=8 rootwait root=/dev/mtdblock5 rootfstype=squashfs mtdoops.mtddev=pstore&quot;; </span><br><span class="line">（blk则为 pstore_blk.blkdev=pstore）</span><br><span class="line"> </span><br><span class="line">blkparts = &quot;mtdparts=spi0.0:64k(spl)ro,256k(uboot)ro,64k(dtb)ro,128k(pstore),3m(kernel)ro,4m(rootfs)ro,-(data)&quot;;</span><br><span class="line"></span><br><span class="line">方案2:</span><br><span class="line">bootargs = &quot;console=ttyS1,115200 loglevel=8 rootwait root=/dev/mtdblock5 rootfstype=squashfs mtdoops.mtddev=pstore&quot;;</span><br><span class="line">在设备树中</span><br><span class="line">partition@60000 &#123;</span><br><span class="line">    label = &quot;pstore&quot;;</span><br><span class="line">    reg = &lt;0x60000 0x20000&gt;;</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure>

<h4 id="挂载pstore文件系统"><a href="#挂载pstore文件系统" class="headerlink" title="挂载pstore文件系统"></a>挂载pstore文件系统</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mount -t pstore pstore /sys/fs/pstore</span><br></pre></td></tr></table></figure>



<h2 id="summary"><a href="#summary" class="headerlink" title="summary"></a>summary</h2><p>以上几种方案都使用到了kmsg_dump的注册机制。 注册很简单，就是把一个全局变量结构挂到一个全局list中。 kmsg_dump是oops时进入kmsg_dump的入口。由panic,die,oops_exit等函数调用。它会一一调用回调函数。 每一个回调函数都会用到kmsg_dump_get_buffer。它先是计算dump还有多少空间，然后把kmsg中最后的一部分写进去。</p>
]]></content>
  </entry>
  <entry>
    <title>linux-cmd</title>
    <url>/linux-cmd.html</url>
    <content><![CDATA[<h4 id="硬链接-软链接-符号链接"><a href="#硬链接-软链接-符号链接" class="headerlink" title="硬链接 软链接 符号链接"></a>硬链接 软链接 符号链接</h4><h6 id="链接"><a href="#链接" class="headerlink" title="链接"></a>链接</h6><p>简单来说是文件共享的方式。</p>
<h6 id="硬链接"><a href="#硬链接" class="headerlink" title="硬链接"></a>硬链接</h6><p>具有同样的索引节点和文件属性，只有链接数&#x3D;0的时候，才会用磁盘删除内容。</p>
<ul>
<li><p>不允许给目录创建硬链接。	</p>
</li>
<li><p>不可以给不同文件系统的文件间建立连接。因为 inode节点 是这个文件在当前分区中的索引值，是相对于这个分区的，当然不能跨越文件系统了。</p>
</li>
</ul>
<h6 id="软链接"><a href="#软链接" class="headerlink" title="软链接"></a>软链接</h6><h6 id="符号链接"><a href="#符号链接" class="headerlink" title="符号链接"></a>符号链接</h6><p>软链接也叫做符号链接。没有文件系统限制。软链接原文件&#x2F;链接文件拥有不同的inode号，表明他们是两个不同的文件；建立软链接就是建立了一个新文件。当访问链接文件时，系统就会发现他是个链接文件，它读取链接文件找到真正要访问的文件。</p>
<ul>
<li><p>软链接的链接数目不会增加；</p>
</li>
<li><p>可以给目录增加链接。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
  </entry>
  <entry>
    <title>linux_time_Cfun</title>
    <url>/linux-time-Cfun.html</url>
    <content><![CDATA[<h3 id="linux下时间处理"><a href="#linux下时间处理" class="headerlink" title="linux下时间处理"></a>linux下时间处理</h3><h4 id="格林尼治时间"><a href="#格林尼治时间" class="headerlink" title="格林尼治时间"></a>格林尼治时间</h4><ul>
<li>所有的UNIX系统都使用同一个时间和日期的起点：格林尼治时间（GMT）1970年1月1 日午夜（0点）。这是“UNIX纪元的起点”，Linux也不例外。Linux系统中所有的时间都以从那时起经过的秒数来衡量。</li>
</ul>
<h4 id="时间转换关系"><a href="#时间转换关系" class="headerlink" title="时间转换关系"></a>时间转换关系</h4><p><img src="/../image/image-20240301170335842.png" alt="image-20240301170335842"></p>
<p>其中，虚线表示的三个函数localtime,mktime,strftime受到环境变量TZ的影响，如果定义了TZ，则使用其，如果没有，则使用协调统一时间UTC。</p>
<h4 id="timespec结构体"><a href="#timespec结构体" class="headerlink" title="timespec结构体"></a>timespec结构体</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct timespec</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line">    time_t tv_sec;  秒数</span><br><span class="line">    long tv_nsec;   纳秒</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="time函数"><a href="#time函数" class="headerlink" title="time函数"></a>time函数</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;time.h&gt;</span><br><span class="line">time_t time(time_t *t);</span><br><span class="line"> </span><br><span class="line">//返回值：成功返回时间值；出错返回-1</span><br></pre></td></tr></table></figure>

<p>调用time函数得到底层的时间值，它<strong>返回的是从格林尼治时间****开始至今的秒数</strong></p>
<p>time函数还会把返回值写入t指针指向的位置</p>
<h4 id="difftime函数"><a href="#difftime函数" class="headerlink" title="difftime函数"></a>difftime函数</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;time.h&gt;</span><br><span class="line"> </span><br><span class="line">double difftime(time_t time1, time_t time0);</span><br></pre></td></tr></table></figure>

<p>该函数用<strong>来计算两个time_t值之间的秒数并以double类型返回它</strong>。</p>
<h4 id="时钟接口函数（clock-gettime、clock-getres、clock-settime）"><a href="#时钟接口函数（clock-gettime、clock-getres、clock-settime）" class="headerlink" title="时钟接口函数（clock_gettime、clock_getres、clock_settime）"></a>时钟接口函数（clock_gettime、clock_getres、clock_settime）</h4><p>时钟通过clockid_t类型进行标识，下面给出了标准值</p>
<p><img src="/../image/image-20240301171937859.png" alt="image-20240301171937859"></p>
<h5 id="clock-gettime函数"><a href="#clock-gettime函数" class="headerlink" title="clock_gettime函数"></a>clock_gettime函数</h5><ul>
<li>该函数可用于获取指定时钟的时间，将获取的时钟时间存放在参数2中</li>
<li>当时钟ID设置为<strong>CLOCK_REALTIME时</strong>，clock_gettime函数提供了<strong>与time函数类似的功能</strong>，不过在系统支持高精度时间值的情况下，clock_gettime可能比time函数得到<strong>更高精度的时间值</strong></li>
</ul>
<h5 id="clock-getres函数"><a href="#clock-getres函数" class="headerlink" title="clock_getres函数"></a>clock_getres函数</h5><ul>
<li>该函数把参数把参数tsp指向的timespec结构初始化为与clock_id参数对应的时钟精度</li>
<li>例如：如果精度为1毫秒，则timespec结构体的tv_sec字段就被设置为0，tv_nsec字段就被设置为1000000</li>
</ul>
<h5 id="clock-settime函数"><a href="#clock-settime函数" class="headerlink" title="clock_settime函数"></a>clock_settime函数</h5><ul>
<li>要对特定的时钟设置时间，可以调用clock_settime函数</li>
<li>我们需要适当的特权来更改时钟值，但是有些时钟是不能修改的</li>
</ul>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
  </entry>
  <entry>
    <title>linux-udev</title>
    <url>/linux-udev.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>makefile</title>
    <url>/makefile.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>mcu_cold_restart_question</title>
    <url>/mcu-cold-restart-question.html</url>
    <content><![CDATA[<h4 id="转载-–-单片机冷启动下重启问题分析"><a href="#转载-–-单片机冷启动下重启问题分析" class="headerlink" title="转载 –&gt; 单片机冷启动下重启问题分析"></a>转载 –&gt; 单片机冷启动下重启问题分析</h4><h4 id="问题现象"><a href="#问题现象" class="headerlink" title="问题现象"></a>问题现象</h4><p>客户反馈MCU作为他们产品的控制芯片，在常温下工作是正常的，但是稍微冷冻下就会启动失败，重现率100%，再次加热或者恢复到常温又能正常工作。</p>
<h4 id="问题分析解决"><a href="#问题分析解决" class="headerlink" title="问题分析解决"></a>问题分析解决</h4><p>从客户描述上来看，猜测很大可能是硬件问题，因此带了一块STM32F030-NUCLEO板过去，想着做个芯片交换测试看下结果。</p>
<p>到达客户现场，了解到客户只是使用了内部高速晶振HSI。先使用示波器抓下VDD和NRST的启动波形，在常温下发现并没有明显异常。于是做低温测试，为了对比，基于STM32F030-NUCLEO板了写了一个只使用内部高速晶振HSI , 翻转一个LED灯的程序。</p>
<p>结果显示，STM32F030-NUCLEO板能正常启动，而客户的板子问题重现，再次测量其VDD和NRST的启动波形，发现VDD上电过程中有稍微不规则波形，但感觉不至于导致MCU无法启动。考虑到当前客户板子上的MCU跑的是客户自己的程序，为了统一对比，将客户板子上的MCU烧录成NUCELO板上一样的程序，再次做低温测试，结果显示客户的板子也能正常启动！</p>
<p>于是可以初步断定，此问题与客户自己的软件有关，而与外围电路无关。</p>
<p>接下来对比测试代码与客户自己的代码，并再次做低温测试验证结果，最终发现客户的时钟树配置有个参数有问题：</p>
<p><img src="/../image/image-20240329095021747.png" alt="image-20240329095021747"></p>
<p>如上红色代码所示，RCC_OSCILLATORTYPE_NONE改成RCC_OSCILLATORTYPE_HSI后。问题现象明显改善，但经过测试，发现偶尔还会启动不正常的时候。但相对于之前100%可以重现的现象，至少说明之前软件的失误至少是一个因素。</p>
<p>现在问题变成偶尔重现，已经向前迈进一大步。接下来怀疑与硬件有关了，理由是同样的测试软件跑在用户的板子上和跑在NUCELO软件上的结果不一致。</p>
<p>因此接下来首先对于用户的板子的外围电路与STM32F030-NUCLEO板子的外围电路，发现客户MCU的BOOT0引脚是悬空的，于是加上一个外部10K下拉电阻，再次测试问题不再重现。</p>
<p>至此，问题解决！</p>
<h4 id="后话"><a href="#后话" class="headerlink" title="后话"></a>后话</h4><p>回过头来看这个问题，发现客户知道MCU使用的是HSI，可偏偏在代码中配置时钟树时使用的晶振类型却是NONE !这种问题现在看来是非常低级的问题，但在代码量大，或者代码迭代的过程中，之前写代码的人离职，后续接手的工程师又不能全盘了解所有代码的情况下时就会变得束手无策，当碰到此类莫名其妙的问题，特别是无法判断到底是硬件问题还是软件问题的时候，保持清晰的思路是非常重要的。（另外，对于这个现象，<strong>看门狗</strong>也是可能是一种思路。比如说冷启动的时候导致看门狗模块异常不能及时喂狗所导致的重启）。继续补充一下。<strong>在低温环境，要注意晶振负载电容随温度的变化</strong>。</p>
<p>这里我需要强调的是，最有效的解决方法就是<strong>快速找到一个 “参照物”</strong>，而ST的DEMO板和示例代码就是在硬件上和软件上扮演这样一个参照物的角色。<strong>可以通过MCU交换测试来判断是不是芯片外围电路的问题或者芯片问题</strong>，可以使用Cube库下的示例代码，对比其运行结果来判断是否与软件有关。<mark>先从大方向明确问题到底是与硬件有关还是与软件有关，然后再做下一步分析</mark>，这种方法希望读者能有效掌握。</p>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>转载这篇文章，对思路进行一下开阔，有的问题可能看似与软件无关，实则确实息息相关。遇到异常的时候，首先分析<mark>时钟，电源，复位</mark>是否都是正常，不论是从软件还是从硬件上来确认。</p>
]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>mcu_option_byte</title>
    <url>/mcu-option-byte.html</url>
    <content><![CDATA[<h4 id="option-byte介绍"><a href="#option-byte介绍" class="headerlink" title="option byte介绍"></a>option byte介绍</h4>]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>mcu_reset</title>
    <url>/mcu-reset.html</url>
    <content><![CDATA[<h4 id="单片机复位介绍"><a href="#单片机复位介绍" class="headerlink" title="单片机复位介绍"></a>单片机复位介绍</h4><p>经典的STM32内部复位电路如下</p>
<p><img src="/../image/image-20240410090253667.png" alt="image-20240410090253667"></p>
<p>可以看到复位分为两大类：<strong>内部复位和外部复位</strong></p>
<ul>
<li><p>内部复位源通过脉冲发生器在 NRST 引脚产生延时至少 20μs 的脉冲，引起 NRST 保持电平产生复位；</p>
</li>
<li><p>外部复位源则直接将 NRST 引脚电平拉低产生复 位。</p>
</li>
</ul>
<h5 id="外部复位"><a href="#外部复位" class="headerlink" title="外部复位"></a>外部复位</h5><p>NRST 引脚上的低电平。</p>
<h5 id="软件复位"><a href="#软件复位" class="headerlink" title="软件复位"></a>软件复位</h5><p>将 Arm® Cortex®-M3 中断应用和复位控制寄存器中的 SYSRESETREQ 置“1” 时，可实现软件复位。JLINK对设备的复位就是控制的这一位。Reset: Reset device via AIRCR.SYSRESETREQ.</p>
<h6 id="内核复位"><a href="#内核复位" class="headerlink" title="内核复位"></a>内核复位</h6><p>在Cortex-M内核文档中大概有这样的描述：<strong>通过设置 NVIC 中应用程序中断与复位控制寄存器(AIRCR)的VECTRESET 位，可只复位处理器内核而不复位其它片上设施。</strong></p>
<p>也就是说，这样操作只复位Cortex-M内核，不会复位UART这些片内外设。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">void NVIC_CoreReset(void)</span><br><span class="line">&#123;</span><br><span class="line">  __DSB();</span><br><span class="line">  SCB-&gt;AIRCR  = ((0x5FA &lt;&lt; SCB_AIRCR_VECTKEY_Pos)      |</span><br><span class="line">                 (SCB-&gt;AIRCR &amp; SCB_AIRCR_PRIGROUP_Msk) |</span><br><span class="line">                 SCB_AIRCR_VECTRESET_Msk);       //置位 VECTRESET</span><br><span class="line">  __DSB();</span><br><span class="line">  while(1) &#123; __NOP(); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h6 id="系统复位"><a href="#系统复位" class="headerlink" title="系统复位"></a>系统复位</h6><p>软件复位中的系统复位操作的寄存器位（SYSRESETREQ）不同，复位的对象为整个芯片（除后备区域）。</p>
<p>系统复位函数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">void NVIC_SysReset(void)</span><br><span class="line">&#123;</span><br><span class="line">  __DSB();</span><br><span class="line">  SCB-&gt;AIRCR  = ((0x5FA &lt;&lt; SCB_AIRCR_VECTKEY_Pos)      | </span><br><span class="line">                 (SCB-&gt;AIRCR &amp; SCB_AIRCR_PRIGROUP_Msk) | </span><br><span class="line">                 SCB_AIRCR_SYSRESETREQ_Msk);     //置位 SYSRESETREQ</span><br><span class="line">  __DSB();</span><br><span class="line">  while(1) &#123; __NOP(); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="电源复位"><a href="#电源复位" class="headerlink" title="电源复位"></a>电源复位</h5><p>分为上电复位（POR 复位），掉电复位（PDR 复位），从待机模式唤醒。</p>
<p>依靠单片机内部的电源监控电路，在供电电压达不到设定值时自动进行复位。当检测到 VDD&#x2F;VDDA低于阈值电压 VPOR和 VPDR 时，芯片将会自动保持为复位状 态，上电复位和掉电复位的波形图如下。</p>
<p><img src="/../image/image-20240410114131548.png" alt="image-20240410114131548"></p>
<h5 id="低功耗复位"><a href="#低功耗复位" class="headerlink" title="低功耗复位"></a>低功耗复位</h5><p>低功耗管理复位的产生有两种情况，一种是进入待机模式时，另一种是进入停止 模式时。在这两种情况下，如果把用户选择字节中的 RSTSTDB 位（待机模式 时）或 RSTSTOP 位（停止模式时）清零，系统将被复位而不是进入待机模式或 停止模式。</p>
<h5 id="复位源查看"><a href="#复位源查看" class="headerlink" title="复位源查看"></a>复位源查看</h5><p>可以通过查看 RCM_CSTS（控制&#x2F;状态寄存器）中的复位标志位识别复位事件来源，也可以相应清除标志位。</p>
<p><img src="/../image/image-20240410114427398.png" alt="image-20240410114427398"></p>
]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>mcu-voltage</title>
    <url>/mcu-voltage.html</url>
    <content><![CDATA[<h2 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h2><p>（1）VCC：C&#x3D;circuit 表示电路的意思, 即接入电路的电压</p>
<p>（2）VDD：D&#x3D;device 表示器件的意思, 即器件内部的工作电压;</p>
<p>（3）VSS：S&#x3D;series 表示公共连接的意思，通常指电路公共接地端电压</p>
<p>（4）VEE：负电压供电;场效应管的源极(S)</p>
<p>（5）VBAT：当使用电池或其他电源连接到VBAT脚上时，当VDD 断电时，可以保存备份寄存器的内容和维持RTC的功能。如果应用中没有使用外部电池，VBAT引脚应接到VDD引脚上。</p>
<p>（6）VPP：编程&#x2F;擦除电压。</p>
<p>（7）GND：在电路里常被定为电压参考基点。</p>
<p>（8）CC与DD的区别是：供电电压与工作电压的区别（通常VCC&gt;VDD）</p>
<p>（9）V与VA的区别是：数字与模拟的区别</p>
<blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- 数字电路供电VCC</span><br><span class="line">- 模拟电路供电VCCA</span><br><span class="line">- VDD是指数字工作电压，就是供电进芯片的</span><br><span class="line">- VDDA是模拟电压或者叫模拟正电源，是从芯片向外供电的</span><br></pre></td></tr></table></figure>
</blockquote>
<h2 id="应用解释"><a href="#应用解释" class="headerlink" title="应用解释"></a>应用解释</h2><p>1、对于数字电路来说，VCC是电路的供电电压,VDD是芯片的工作电压(通常Vcc&gt;Vdd)，VSS是接地点。例如，对于ARM单片机来说，其供电电压VCC一般为5V，一般经过稳压模块将其转换为单片机工作电压VDD &#x3D; 3.3V</p>
<p>2、有些IC既带VDD引脚又有VCC引脚，说明这种器件自身带有电压转换功能。</p>
<p>3、在场效应管(或COMS器件)中，VDD为漏极，VSS为源极，VDD和VSS指的是元件引脚，而不表示供电电压。</p>
<p>4、一般来说VCC&#x3D;模拟电源,VDD&#x3D;数字电源,VSS&#x3D;数字地,VEE&#x3D;负电源</p>
<p>5、从电气意义上说，GND分为电源地和信号地。PG是 Power Ground（电源地）的缩写。另一个是 Signal Ground（信号地）。实际上它们可能是连在一起的（不一定是混在一起哦！）。两个名称主要是便于对电路进行分析。</p>
<p>进一步说，还有因电路形式不同而必须区分的两种“地”：数字地，模拟地。</p>
<p>数字地和模拟地都有信号地、电源地两种情况。数字地和模拟地之间，某些电路可以直接连接，有些电路要用电抗器连接，有些电路不可连接。</p>
]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>mma</title>
    <url>/mma.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>netlink</title>
    <url>/netlink.html</url>
    <content><![CDATA[<h1 id="什么是netlink？"><a href="#什么是netlink？" class="headerlink" title="什么是netlink？"></a>什么是netlink？</h1><p>netlink提供的是内核态和用户态之间的通信机制，也可以用于用户态和用户态的两个进程之间通信。</p>
<h3 id="netlink的优势"><a href="#netlink的优势" class="headerlink" title="netlink的优势"></a>netlink的优势</h3><p>一般的用户态和内核空间通信方式有三种，ioctl，proc，netlink。而前两种都是<strong>单向的，</strong>而netlink可以实现<strong>双向通信。</strong>基于BSD socket和AF_NETLINK协议簇。使用32位的端口号寻址。每个netlink协议，通常与一个或者一组内核服务&#x2F;组件相关联。NETLINK_ROUTE用于获取和设置路由与链路信息、NETLINK_KOBJECT_UEVENT用于内核向用户空间的udev进程发送通知等。</p>
<h3 id="netlink的特点"><a href="#netlink的特点" class="headerlink" title="netlink的特点"></a>netlink的特点</h3><ul>
<li>支持全双工，异步通信</li>
<li>在内核空间使用专门的内核api接口</li>
<li>支持多播，可以实现总线式订阅</li>
<li>在内核端可以用于进程上下文和中断上下文</li>
<li>用户空间使用标准BSD Socket接口</li>
</ul>
<h3 id="关键数据结构"><a href="#关键数据结构" class="headerlink" title="关键数据结构"></a>关键数据结构</h3><p><img src="/../image/image-20240805200041276.png" alt="image-20240805200041276"></p>
<h5 id="msghdr"><a href="#msghdr" class="headerlink" title="msghdr"></a>msghdr</h5><p>我们知道socket消息的发送和接收函数一般有这几对：recv／send、readv／writev、recvfrom／sendto。当然还有recvmsg／sendmsg，前面三对函数各有各的特点功能，而recvmsg／sendmsg就是要囊括前面三对的所有功能，当然还有自己特殊的用途。msghdr的前两个成员就是为了满足recvfrom／sendto的功能，中间两个成员msg_iov和msg_iovlen则是为了满足readv／writev的功能，而最后的msg_flags则是为了满足recv／send中flag的功能，剩下的msg_control和msg_controllen则是满足recvmsg／sendmsg特有的功能。</p>
<h5 id="sockaddr-nl"><a href="#sockaddr-nl" class="headerlink" title="sockaddr_nl"></a>sockaddr_nl</h5><p><img src="/../image/image-20240805200415939.png" alt="image-20240805200415939"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct sockaddr_nl</span><br><span class="line">&#123;</span><br><span class="line">    sa_family_t nl_family; /*该字段总是为AF_NETLINK */</span><br><span class="line">    unsigned short nl_pad; /* 目前未用到，填充为0*/</span><br><span class="line">    __u32 nl_pid; /* process pid */</span><br><span class="line">    __u32 nl_groups; /* multicast groups mask */为0代表不希望加入任何多播组</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h5 id="struct-nlmsghdr"><a href="#struct-nlmsghdr" class="headerlink" title="struct nlmsghdr"></a><strong>struct nlmsghdr</strong></h5><p>netlink报文消息由头和消息体构成。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct nlmsghdr</span><br><span class="line">&#123;</span><br><span class="line">    __u32 nlmsg_len; /* Length of message including header */ 整个消息的长度，按字节计算。包括了Netlink消息头本身。</span><br><span class="line">    __u16 nlmsg_type; /* Message content */ 消息的类型，数据消息还是控制消息</span><br><span class="line">    __u16 nlmsg_flags; /* Additional flags */ 附加的额外信息</span><br><span class="line">    __u32 nlmsg_seq; /* Sequence number */</span><br><span class="line">    __u32 nlmsg_pid; /* Sending process PID */</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h1 id="用户空间使用netlink"><a href="#用户空间使用netlink" class="headerlink" title="用户空间使用netlink"></a>用户空间使用netlink</h1><h5 id="1-创建socket"><a href="#1-创建socket" class="headerlink" title="1.创建socket"></a>1.创建socket</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int socket(int domain, int type, int protocol)</span><br><span class="line">domain指代地址族,即AF_NETLINK;</span><br><span class="line">套接字类型为SOCK_RAW或SOCK_DGRAM,因为netlink是一个面向数据报的服务;</span><br><span class="line">protocol选择该套接字使用哪种netlink特征。</span><br></pre></td></tr></table></figure>

<h5 id="2-绑定netlink"><a href="#2-绑定netlink" class="headerlink" title="2.绑定netlink"></a>2.绑定netlink</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bind(fd, (struct sockaddr*)&amp;, nladdr, sizeof(nladdr));</span><br></pre></td></tr></table></figure>

<h5 id="3-发送netlink消息"><a href="#3-发送netlink消息" class="headerlink" title="3.发送netlink消息"></a>3.发送netlink消息</h5><p>为了发送一条netlink消息到内核或者其他的用户空间进程,另外一个struct sockaddr_nl nladdr需要作为目的地址,这和使用sendmsg()发送一个UDP包是一样的。</p>
<ul>
<li>如果该消息是发送至内核的,那么nl_pid和nl_groups都置为0.</li>
<li>如果消息是发送给另一个进程的单播消息,nl_pid是另外一个进程的pid值而nl_groups为零。</li>
<li>如果消息是发送给一个或多个多播组的多播消息,所有的目的多播组必须bitmask必须or起来从而形成nl_groups域。sendmsg(fd, &amp;, msg, 0);</li>
</ul>
<h5 id="4-接收netlink消息"><a href="#4-接收netlink消息" class="headerlink" title="4.接收netlink消息"></a>4.接收netlink消息</h5><p>一个接收程序必须分配一个足够大的内存用于保存netlink消息头和消息负载。然后其填充struct msghdr msg,再使用标准的recvmsg()函数来接收netlink消息。</p>
<p>当消息被正确的接收之后,nlh应该指向刚刚接收到的netlink消息的头。nladdr应该包含接收消息的目的地址,其中包括了消息发送者的pid和多播组。同时,宏NLMSG_DATA(nlh),定义在netlink.h中,返回一个指向netlink消息负载的指针。调用close(fd)关闭fd描述符所标识的socket；recvmsg(fd, &amp;, msg, 0);</p>
<h1 id="内核空间使用netlink"><a href="#内核空间使用netlink" class="headerlink" title="内核空间使用netlink"></a>内核空间使用netlink</h1><h5 id="创建netlink-socket"><a href="#创建netlink-socket" class="headerlink" title="创建netlink socket"></a>创建netlink socket</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct sock *netlink_kernel_create(struct net *net,</span><br><span class="line">                                   int unit,unsigned int groups,</span><br><span class="line">                                   void (*input)(struct sk_buff *skb),</span><br><span class="line">                                   struct mutex *cb_mutex,struct module *module);</span><br></pre></td></tr></table></figure>

<h5 id="发送单播信息"><a href="#发送单播信息" class="headerlink" title="发送单播信息"></a>发送单播信息</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int netlink_unicast(struct sock *ssk, struct sk_buff *skb, u32 pid, int nonblock)</span><br><span class="line">ssk为kernel create返回的socket，skb存放消息，data段指向netlink消息结构，skb控制块保存了消息的地址信息。pid为接收消息的进程信息。nonblock表示是否阻塞。</span><br></pre></td></tr></table></figure>

<h5 id="发送广播信息"><a href="#发送广播信息" class="headerlink" title="发送广播信息"></a>发送广播信息</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int netlink_broadcast(struct sock *ssk, struct sk_buff *skb, u32 pid, u32 group, gfp_t allocation)</span><br></pre></td></tr></table></figure>

<h5 id="释放netlink-socket"><a href="#释放netlink-socket" class="headerlink" title="释放netlink socket"></a>释放netlink socket</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int netlink_broadcast(struct sock *ssk, struct sk_buff *skb, u32 pid, u32 group, gfp_t allocation)</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>networkcard-cfg</title>
    <url>/networkcard-cfg.html</url>
    <content><![CDATA[<h4 id="Linux下网卡命名规则"><a href="#Linux下网卡命名规则" class="headerlink" title="Linux下网卡命名规则"></a>Linux下网卡命名规则</h4><h5 id="rename-规则"><a href="#rename-规则" class="headerlink" title="rename 规则"></a>rename 规则</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">step1 依据/usr/lib/udev/rules.d/60-net.rules， 查看是否有ifcfg-xx配置文件（路径在/etc/sysconfig/network-scripts/),</span><br><span class="line">是否有定义了指定MAC地址的配置文件（ifcfg-xx ，xx必须和配置文件的内容DEVICE一致），如果有，则命名改网卡；</span><br><span class="line"></span><br><span class="line">step2 依据/usr/lib/udev/rules.d/71-biosdevname.rules，如果biosdevname使能了（安装了biosdevname这个包，且内核启动参数显式设置为1），</span><br><span class="line">且网卡没有在step1中定义，则按照biosdevname命名规则rename网卡；（注意，如果没有安装biosdevname这个包，就没有这个文件）</span><br><span class="line"></span><br><span class="line">step3, 依据/lib/udev/rules.d/75-net-description.rules，将udev工具会根据device属性将填写网卡的属性命名，可能一个网卡会有多个维度的名称;</span><br><span class="line"></span><br><span class="line">step4，udev 根据step3中的赋值，按照指定的scheme规则，去给在step1 step2中没有命名的网卡命名;</span><br><span class="line"></span><br><span class="line">强调：这个step顺序是在我们没有自定义自己的rules的前提下，如果用户自定义了自己的rules，则用户自定义为优先级最高</span><br></pre></td></tr></table></figure>

<h5 id="传统命名"><a href="#传统命名" class="headerlink" title="传统命名"></a>传统命名</h5><p>centos6之前采用的都是传统的命名方式，如eth0,1…</p>
<h5 id="可预测的命名"><a href="#可预测的命名" class="headerlink" title="可预测的命名"></a>可预测的命名</h5><p>cenos7之后提供了可预测性的网卡命名方式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">如果从BIOS中能够取到可用的，板载网卡的索引号，则使用这个索引号命名，例如: eno1，如不能则尝试2</span><br><span class="line">如果从BIOS中能够取到可以用的，网卡所在的PCI-E热插拔插槽(注：pci槽位号)的索引号，则使用这个索引号命名，例如: ens1，如不能则尝试3</span><br><span class="line">如果能拿到设备所连接的物理位置（PCI总线号+槽位号？）信息，则使用这个信息命名，例如:enp2s0，如不能则尝试4</span><br><span class="line">传统的kernel命名方法，例如: eth0，这种命名方法的结果不可预知的，即可能第二块网卡对应eth0，第一块网卡对应eth1</span><br><span class="line">使用网卡的MAC地址来命名，这个方法一般不使用</span><br></pre></td></tr></table></figure>

<h5 id="自定义规则"><a href="#自定义规则" class="headerlink" title="自定义规则"></a>自定义规则</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在用户没有自定义rules文件前提下，step1中的网卡命名方式也可认为是一种用户自定义的网卡命名，</span><br><span class="line">即在/etc/sysconfig/network-scripts/ifcfg-xx 文件，xx就是这个网卡名称，文件内容中体现MAC_ADDRESS、NAME，</span><br><span class="line">这种情况下，则会按照配置文件中指定的名称来命名网卡</span><br><span class="line"></span><br><span class="line">如果用户自定义了rules文件，放在/etc/udev/rules.d/目录下，则这个优先级是最高的；</span><br><span class="line">比1中ifcfg-xx方式优先级更高，但是如果两者不一致，则在重启network服务时，会依据ifcfg-xx，</span><br><span class="line">所以用户不应该同时采用里两种方式给同一个网卡命不同的名称</span><br></pre></td></tr></table></figure>

<p>udevadm info &#x2F;sys&#x2F;class&#x2F;net&#x2F;网卡名</p>
<p>可以显示此时这个网卡的pcie信息，vendor_id,idbus等等信息</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">root@Bai5gc:/sys/class/net/eth1# udevadm info /sys/class/net/eth1</span><br><span class="line">P: /devices/pci0000:00/0000:00:02.2/0000:03:00.0/net/eth1</span><br><span class="line">E: DEVPATH=/devices/pci0000:00/0000:00:02.2/0000:03:00.0/net/eth1</span><br><span class="line">E: ID_BUS=pci</span><br><span class="line">E: ID_MODEL_FROM_DATABASE=Ethernet Connection X552 10 GbE Backplane</span><br><span class="line">E: ID_MODEL_ID=0x15ab</span><br><span class="line">E: ID_NET_DRIVER=ixgbe</span><br><span class="line">E: ID_NET_LINK_FILE=/lib/systemd/network/99-default.link</span><br><span class="line">E: ID_NET_NAME_MAC=enxb4a9fca897e7</span><br><span class="line">E: ID_NET_NAME_ONBOARD=eno3</span><br><span class="line">E: ID_NET_NAME_PATH=enp3s0f0</span><br><span class="line">E: ID_PATH=pci-0000:03:00.0</span><br><span class="line">E: ID_PATH_TAG=pci-0000_03_00_0</span><br><span class="line">E: ID_PCI_CLASS_FROM_DATABASE=Network controller</span><br><span class="line">E: ID_PCI_SUBCLASS_FROM_DATABASE=Ethernet controller</span><br><span class="line">E: ID_VENDOR_FROM_DATABASE=Intel Corporation</span><br><span class="line">E: ID_VENDOR_ID=0x8086</span><br><span class="line">E: IFINDEX=3</span><br><span class="line">E: INTERFACE=eth1</span><br><span class="line">E: SUBSYSTEM=net</span><br><span class="line">E: SYSTEMD_ALIAS=/sys/subsystem/net/devices/eth1</span><br><span class="line">E: TAGS=:systemd:</span><br><span class="line">E: USEC_INITIALIZED=5061037</span><br><span class="line">E: net.ifnames=0</span><br></pre></td></tr></table></figure>

<blockquote>
<p>ps：解释一下pci地址</p>
<ul>
<li><code>/pci0000:00</code>：这是根 PCIe 控制器的地址，<code>pci0000:00</code> 表示根控制器。</li>
<li><code>/0000:00:02.2</code>：这是连接到根控制器的第一个 PCIe 设备的地址。<code>0000:00:02.2</code> 表示这个设备的总线号、设备号和功能号。</li>
<li><code>/0000:03:00.0</code>：这是连接到第一个 PCIe 设备的下一个 PCIe 设备的地址。<code>0000:03:00.0</code> 表示新设备的总线号、设备号和功能号。</li>
</ul>
</blockquote>
<h5 id="命名rule-d"><a href="#命名rule-d" class="headerlink" title="命名rule.d"></a>命名rule.d</h5><h6 id="biosdevname和net-ifnames两种命名规范"><a href="#biosdevname和net-ifnames两种命名规范" class="headerlink" title="biosdevname和net.ifnames两种命名规范"></a>biosdevname和net.ifnames两种命名规范</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">net.ifnames的命名规范为:设备类型+设备位置+数字</span><br><span class="line"></span><br><span class="line">设备类型：</span><br><span class="line"></span><br><span class="line">en 表示Ethernet</span><br><span class="line">wl 表示WLAN</span><br><span class="line">ww 表示无线广域网WWAN</span><br><span class="line"></span><br><span class="line">实际的例子:</span><br><span class="line"></span><br><span class="line">eno1 板载网卡</span><br><span class="line">enp0s2  pci网卡</span><br><span class="line">ens33   pci网卡</span><br><span class="line">wlp3s0  PCI无线网卡</span><br><span class="line">wwp0s29f7u2i2   4G modem</span><br><span class="line">wlp0s2f1u4u1   连接在USB Hub上的无线网卡</span><br><span class="line">enx78e7d1ea46da pci网卡</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">biosdevname的命名规范为: 要么是em开头，要么是p开头</span><br><span class="line">实际的例子:</span><br><span class="line"></span><br><span class="line">em1 板载网卡</span><br><span class="line">p3p4 pci网卡</span><br><span class="line">p3p4_1 虚拟网卡</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
  </entry>
  <entry>
    <title>padmux</title>
    <url>/padmux.html</url>
    <content><![CDATA[<h1 id="padmux引脚复用"><a href="#padmux引脚复用" class="headerlink" title="padmux引脚复用"></a>padmux引脚复用</h1><p>对于sstar芯片。</p>
<p>pad mux dtsi文件路径为kernel&#x2F;arch&#x2F;arm&#x2F;boot&#x2F;dts&#x2F;xxx-padmux.dtsi。根据项目需求，修改该文件即可设置pin脚的复用模式。</p>
<p><img src="/../image/image-20240808142459883.png" alt="image-20240808142459883"></p>
<p>第一列为pin脚名称，第二列为pin脚复用模式，参考m_stPadMuxTbl 数组中的mode选项，第三列为复用功能中具体的pin脚作用，参考<code>mdrv_puse.h</code>。</p>
<h5 id="gpio映射表"><a href="#gpio映射表" class="headerlink" title="gpio映射表"></a>gpio映射表</h5><p>每一个chip都有一个gpio-mapping-table.xlx文件，里面有相关gpio设置。</p>
<h5 id="padmux表"><a href="#padmux表" class="headerlink" title="padmux表"></a>padmux表</h5><p>每个pin脚具体的复用功能也可以见mhal_pinmux.c的m_stPadMuxTbl数组。复用关系优先级从高到低，gpio功能的优先级最低。<strong>复用某个pin脚的某个功能前要确认高优先级复用是关闭的。</strong></p>
<p><img src="/../image/image-20240808142923826.png" alt="image-20240808142923826"></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PAD_FUART_TX：        引脚</span><br><span class="line">CHIPTOP_BANK：        寄存器地址</span><br><span class="line">REG_SR_PCK_MODE：     偏移地址</span><br><span class="line">REG_PWM0_MODE_MASK    mask位</span><br><span class="line">BIT1|BIT0             对应的设置bit位</span><br><span class="line">PINMUX_FOR_PWM0_MODE_3： 模式</span><br></pre></td></tr></table></figure>

<h5 id="设置gpio9为gpio模式"><a href="#设置gpio9为gpio模式" class="headerlink" title="设置gpio9为gpio模式"></a>设置gpio9为gpio模式</h5><p><img src="/../image/image-20240808143130176.png" alt="image-20240808143130176"></p>
<p>通过读取寄存器来确认复用是否成功</p>
<h5 id="设置i2cs0为pwm模式"><a href="#设置i2cs0为pwm模式" class="headerlink" title="设置i2cs0为pwm模式"></a>设置i2cs0为pwm模式</h5><p><img src="/../image/image-20240808143231127.png" alt="image-20240808143231127"></p>
<p><img src="/../image/image-20240808143325826.png" alt="image-20240808143325826"></p>
]]></content>
  </entry>
  <entry>
    <title>pcie_ltssm</title>
    <url>/pcie_ltssm.html</url>
    <content><![CDATA[<h6 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h6><p>PCIe LTSSM ，全名为Link Training and Status State Machine，主要是用在PCIe中Physical Layer Link的初始化与设置，让device之间建立起沟通桥梁。整个LTSSM状态机总共有11个state，每个state又可以分为多个substates，所以整个状态机的跳转可以说是非常的错综且复杂且完善。</p>
<p><img src="/../image/image-20240228141706297.png" alt="image-20240228141706297"></p>
<h6 id="训练顺序"><a href="#训练顺序" class="headerlink" title="训练顺序"></a>训练顺序</h6><p>Traning Sequences Order Sets主要有两种，分别为TS1和TS2，主要目的是为了建立Bitlock和S ymbol Lock和交换Physical Layer的参数。当Link运作在2.5 GT&#x2F;s(Gen 1)或5 GT&#x2F;s(Gen 2)时，TS1&#x2F;TS2使用8b&#x2F;10b编码，8 GT&#x2F;s则使用128b&#x2F;130b编码。</p>
<h6 id="链路训练速率"><a href="#链路训练速率" class="headerlink" title="链路训练速率"></a>链路训练速率</h6><p>主要为三个Data Rates，分别为2.5GT&#x2F;s，5GT&#x2F;s 和 8GT&#x2F;s。每个link开始必须以2.5GT&#x2F;s的速率开始initial。</p>
<p>在negotiation的过程中，LTSSM会决定PCIe Port的link number和lane number。</p>
<h6 id="LTSSM简介"><a href="#LTSSM简介" class="headerlink" title="LTSSM简介"></a>LTSSM简介</h6><ul>
<li><p>Detect：主要是为了检测对端是否有device存在，分为detect.quiet和detect.active状态。</p>
<ul>
<li><p>Detect.Quiet: Quiet的状态下，Tx会处于Electrical Idle的状态，且起始会以2.5 GT&#x2F;S的速度开始，也就是Gen1 Link Speed。</p>
</li>
<li><p>Detect.Active: 在这个state，会perform一个叫做Receiver Detection Sequence。Receiver Detection Sequence首先会让Transmitter处于一个稳定的电压状态，然后持续的对D+与D-增加电压直到符合V TX-RCV-DETECT，另一放面持续的去侦测Receiver的高阻抗，然后用充电的曲线(rate)来判断是否有device的存在。</p>
</li>
</ul>
</li>
<li><p>Polling：通过传送TS Order Set，回应所接收到的TS Order Set，在这个阶段建立Bit block和Symbol Lock。分为polling.active，polling.compliance和polling.configuration状态。</p>
<ul>
<li>Polling.Active: 在这个状态下，Transmitter会开始传送TS1 Order Set，并且将” Link Number “ &amp; “ Lane number within Link “设置为0，而且所有传送的TS1中的Data Rate Identifier栏位必须将所有support的date rate设为1。</li>
<li>Polling.Compliance: 这个state主要透过示波器、封包产生器或位元错误率测试仪用来做interoperability testing，只有EE想要量测讯号才会需要进入此state，一般的link training是可以跳过这个状态的。</li>
<li>Polling.Configuration: 在上面有提到Polling.Active会传送Lane&#x2F;Link number为PAD&#x2F;PAD的TS1，Polling.Configuration则是传送Lane&#x2F;Link number为PAD&#x2F;PAD的TS2，所以Polling.Configuration的TS2有点像是TS1的Double Confirm 的概念。</li>
</ul>
</li>
<li><p>configuration：主要任务是分配port上所有的lanes，且分组成个别独立的Links，主要是透过TS1,TS2中的Link Number &amp; Lane number within Link来达成，区别于polling state，这里已经被填充为实际的值。</p>
<p><img src="/../image/image-20240228170627139.png" alt="image-20240228170627139"></p>
<ul>
<li><p>配置.链接宽度.开始： </p>
</li>
<li><ul>
<li>DownStream: 由于Downstream不知道现在port的连接如何，所以会以试探性的方式，在所传送出的TS1中的Link number栏位选择任意号码做为初始值，然后Lane number维持PAD。</li>
</ul>
</li>
</ul>
<ul>
<li><ul>
<li>UpStream: 传送TS1，且Link&#x2F;Lane number栏位保持PAD，并且在收到一定数量和条件的TS1后，比DownStream较早进入Linkwidth.Accept。</li>
</ul>
</li>
<li><p>配置.链接宽度.接受： </p>
</li>
<li><ul>
<li>DownStream: 当DownStream接收一定数量的TS1中，Link Number栏位符合Linkwidth.Start state所传出的号码，那就可以形成一个Link Group。</li>
</ul>
</li>
<li><ul>
<li>UpStream: 会根据在DownStream在Linkwidth.Start阶段所发出的Link Number中，选定一个号码作为此port的Link Number，因此会在传送TS1中将Link Number栏位填入这个号码，所以顾名思义，UpStream接受了(Accept)了这个Link Number。</li>
</ul>
</li>
<li><p>配置.Lanenum.等待： </p>
</li>
<li><ul>
<li>DownStream: 由于在Linkwidth.Accept的阶段某些Lanes已经组成一个Link Group，因此DownStream会开始在这个阶段传送独立的Lane Number值到每个Lane上。</li>
</ul>
</li>
<li><ul>
<li>UpStream: 会根据接收到的Lane number，直接回传相同的号码或者不同( Lane Reversal )，这里spec有提到相同或不相同，都还是可以进入下一个state。</li>
</ul>
</li>
<li><p>Configuration.Lanenum.Accept： </p>
</li>
<li><ul>
<li>当接收到TS1 Link&#x2F;Lane number都与自己所传送的相符合，则会进入Configuration.Complete state。</li>
</ul>
</li>
<li><p>配置。完成： </p>
</li>
<li><ul>
<li>接续Lanenum.Accept state，Complete阶段会开始传送相同的Link&#x2F;Lane number，只是与Lanenum.Accept的差异在于Complete传送的是TS2。</li>
</ul>
</li>
<li><p>配置.空闲： </p>
</li>
<li><ul>
<li>在这个state，双方会开始传送Idle Symbols，当接收到连续8个Idle Symbols后，则会进入L0。</li>
</ul>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>pcie</category>
      </categories>
  </entry>
  <entry>
    <title>pll</title>
    <url>/pll.html</url>
    <content><![CDATA[<h4 id="PLL原理"><a href="#PLL原理" class="headerlink" title="PLL原理"></a>PLL原理</h4><p><img src="/../image/image-20240409175353208.png" alt="image-20240409175353208"></p>
<p>由一个<strong>基准频率振荡器</strong>（晶体振荡器），<strong>相位频率比较器</strong>，<strong>VCO</strong>（电压控制振荡器），<strong>回路滤波器</strong>组成。晶振产生频率为fr的时钟，与VCO产生的fo的时钟同时传入相位频率比较器中进行比较，产生比较结果以脉冲波的形式传入回路滤波器，回路滤波器通过低频将脉冲波转变为直流低压VR，传入VCO对fo进行控制，形成反馈控制电路。</p>
<p><img src="/../image/image-20240409175428500.png" alt="image-20240409175428500"></p>
<p>当fr&gt;fo时产生PD为正脉冲，VR变大，当fr&lt;fo时，PD产生负脉冲波，VR减小，形成对fo的闭环控制，最终目的，是<strong>fo&#x3D;&#x3D;fr</strong>。</p>
<h3 id="倍频器"><a href="#倍频器" class="headerlink" title="倍频器"></a>倍频器</h3><p>PLL的主要功能还是倍频。倍频的实现其实很简单，只要在VCO的后面加一个分频器，时参与比较的Fvco&#x3D;（1&#x2F;n）fo,fo’是VCO产生的。这样就实现了Fvco&#x3D;n*fr。</p>
<p><img src="/../image/image-20240409175451397.png" alt="image-20240409175451397"></p>
<h3 id="分频器原理"><a href="#分频器原理" class="headerlink" title="分频器原理"></a>分频器原理</h3><p>分频器原理就是之前上的简单的数电知识，在这就一块说了。<br>主要就说一个<strong>偶数分频</strong>。<br>偶数分频器的实现简单，用计数器在上升沿或者下降沿计数，当计数器的值等于分频系数的一半或等于分频系数时，信号翻转。<br>电路原理是用一个上升沿计数的计数器，每次计数到2时输出信号clkout翻转一次，每次计数到4时clkout再翻转一次，一直周期重复下去。其他的偶数分频器原理也是一样。</p>
]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>python_parser</title>
    <url>/python-parser.html</url>
    <content><![CDATA[<h1 id="argparse介绍"><a href="#argparse介绍" class="headerlink" title="argparse介绍"></a>argparse介绍</h1><blockquote>
<p>argparse 模块是 Python 内置的一个用于&#x3D;&#x3D;命令项选项与参数解析&#x3D;&#x3D;的模块，argparse 模块可以让人轻松编写用户友好的命令行接口。通过在程序中定义好我们需要的参数，然后 argparse 将会从 sys.argv 解析出这些参数。argparse 模块还会自动生成帮助和使用手册，并在用户给程序传入无效参数时报出错误信息。</p>
<p>官方地址：<a href="https://docs.python.org/zh-cn/3/library/argparse.html#argumentparser-objects">https://docs.python.org/zh-cn/3/library/argparse.html#argumentparser-objects</a></p>
</blockquote>
<h1 id="使用介绍"><a href="#使用介绍" class="headerlink" title="使用介绍"></a>使用介绍</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> argparse</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建一个解析器。创建argumentparser对象</span></span><br><span class="line"><span class="comment">#大多数对argumentparser构造方法的调用都会使用description=关键词参数。这个参数简要描述</span></span><br><span class="line">parser = argparse.ArgumentParser(description=<span class="string">&#x27;test&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用add_argument方法添加参数信息是通过调用add_argument方法完成的。</span></span><br><span class="line">parser.add_argument(<span class="string">&#x27;--sparse&#x27;</span>, action=<span class="string">&#x27;store_true&#x27;</span>, default=<span class="literal">False</span>, <span class="built_in">help</span>=<span class="string">&#x27;GAT with sparse version or not.&#x27;</span>)</span><br><span class="line">parser.add_argument(<span class="string">&#x27;--seed&#x27;</span>, <span class="built_in">type</span>=<span class="built_in">int</span>, default=<span class="number">72</span>, <span class="built_in">help</span>=<span class="string">&#x27;Random seed.&#x27;</span>)</span><br><span class="line">parser.add_argument(<span class="string">&#x27;--epochs&#x27;</span>, <span class="built_in">type</span>=<span class="built_in">int</span>, default=<span class="number">10000</span>, <span class="built_in">help</span>=<span class="string">&#x27;Number of epochs to train.&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用parse_args解析添加的参数</span></span><br><span class="line">args = parser.parse_args()</span><br><span class="line"><span class="built_in">print</span>(args.sparse)</span><br><span class="line"><span class="built_in">print</span>(args.seed)</span><br><span class="line"><span class="built_in">print</span>(args.epochs)</span><br></pre></td></tr></table></figure>

<h4 id="add-argument（）"><a href="#add-argument（）" class="headerlink" title="add_argument（）"></a>add_argument（）</h4><p>其中，这个方法定义了如何解析命令行参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ArgumentParser.add_argument(name <span class="keyword">or</span> flags...[, action][, nargs][, const][, default][, <span class="built_in">type</span>][, choices][, required][, <span class="built_in">help</span>][, metavar][, dest])</span><br><span class="line"></span><br><span class="line">name <span class="keyword">or</span> flags:选项字符串的名字或者列表，例如foo或者-f，--foo</span><br><span class="line">avtion:命令行遇到参数时的动作。默认值是store </span><br><span class="line">（action=storce_true命令行遇到参数时的动作，默认是store，只要运行该变量有传参就将该变量设置为true）</span><br><span class="line">store_const:表示赋值为const</span><br><span class="line">append：将遇到的值存储成列表，如果参数重复会保存多个值。</span><br><span class="line">append_const，将参数规范中定义的一个值保存到一个列表；</span><br><span class="line">count，存储遇到的次数；此外，也可以继承 argparse.Action 自定义参数解析；</span><br><span class="line">nargs - 应该读取的命令行参数个数，可以是具体的数字，或者是?号，当不指定值时对于 Positional argument 使用 default，对于 <span class="type">Optional</span> argument 使用 const；或者是 * 号，表示 <span class="number">0</span> 或多个参数；或者是 + 号表示 <span class="number">1</span> 或多个参数。</span><br><span class="line">const - action 和 nargs 所需要的常量值。</span><br><span class="line">default - 不指定参数时的默认值。</span><br><span class="line"><span class="built_in">type</span> - 命令行参数应该被转换成的类型。</span><br><span class="line">choices - 参数可允许的值的一个容器。</span><br><span class="line">required - 可选参数是否可以省略 (仅针对可选参数)。</span><br><span class="line"><span class="built_in">help</span> - 参数的帮助信息，当指定为 argparse.SUPPRESS 时表示不显示该参数的帮助信息.</span><br><span class="line">metavar - 在 usage 说明中的参数名称，对于必选参数默认就是参数名称，对于可选参数默认是全大写的参数名称.</span><br><span class="line">dest - 解析后的参数名称，默认情况下，对于可选参数选取最长的名称，中划线转换为下划线.</span><br></pre></td></tr></table></figure>

<p>命令行下单点调试的方式，python -m pdb xxx.py</p>
<h1 id="补充串口库"><a href="#补充串口库" class="headerlink" title="补充串口库"></a>补充串口库</h1><p>pyserical库可以非常方便和串口的通讯</p>
<p>官方文档</p>
<p><a href="https://pyserial.readthedocs.io/en/latest/pyserial.html">https://pyserial.readthedocs.io/en/latest/pyserial.html</a></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> serial.tools.list_ports</span><br><span class="line"></span><br><span class="line"><span class="comment"># 列出当前连接的串口设备</span></span><br><span class="line">ports_list = <span class="built_in">list</span>(serial.tools.list_ports.comports())</span><br><span class="line"><span class="keyword">for</span> comport <span class="keyword">in</span> ports_list:</span><br><span class="line">    <span class="built_in">print</span>(comport[<span class="number">0</span>], comport[<span class="number">1</span>])</span><br></pre></td></tr></table></figure>

<p>打开串口，参数说明</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__init__(port=None, baudrate=9600, bytesize=EIGHTBITS, parity=PARITY_NONE, stopbits=STOPBITS_ONE, timeout=None, xonxoff=False, rtscts=False, write_timeout=None, dsrdtr=False, inter_byte_timeout=None, exclusive=None)</span><br></pre></td></tr></table></figure>

<p><strong>bytesize :</strong> 数据位，可取值five six seven eight等等</p>
<p><strong>parity :</strong> 校验位，可取值none，even，odd，mask，spcae等等。</p>
<p><strong>stop：</strong>停止位，one，two，five等等</p>
<p><strong>timeout：</strong>超时时间，读超时时间，可取值为 None, 0 或者其他具体数值（支持小数）。当设置为 None 时，表示阻塞式读取，一直读到期望的所有数据才返回；当设置为 0 时，表示非阻塞式读取，无论读取到多少数据都立即返回；当设置为其他数值时，表示设置具体的超时时间（以秒为单位），如果在该时间内没有读取到所有数据，则直接返回</p>
<p><strong>xonxoff：</strong>软件流控。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#举例 </span></span><br><span class="line"><span class="keyword">import</span> serial.tools.list_ports </span><br><span class="line">s = serial.Serial(<span class="string">&#x27;COM75&#x27;</span>, <span class="number">115200</span>, timeout=<span class="number">0.5</span>) </span><br><span class="line"><span class="built_in">print</span>(s.isOpen())  <span class="comment"># True</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#发送数据 发送数据使用write方法，发送的数据只能是byte类型，因此需要对发送的字符串进行编码操作</span></span><br><span class="line">cmd = <span class="string">&#x27;ifconfig\r\n&#x27;</span>  <span class="comment"># 命令带换行符</span></span><br><span class="line">length = s.write(cmd.encode(<span class="string">&#x27;utf-8&#x27;</span>))</span><br><span class="line"><span class="comment"># 发送字符串</span></span><br><span class="line"><span class="built_in">str</span> = <span class="string">&quot;serial&quot;</span></span><br><span class="line">ser.write(<span class="built_in">str</span>.encode())</span><br><span class="line"><span class="comment"># 发送 bytes</span></span><br><span class="line">data = [<span class="number">0x55</span>,<span class="number">0xAA</span>,<span class="number">0x5A</span>]</span><br><span class="line">ser.write(data)</span><br><span class="line"><span class="comment"># 发送数字</span></span><br><span class="line">num = <span class="number">100</span></span><br><span class="line">ser.write(num.to_bytes(<span class="number">1</span>,<span class="string">&quot;little&quot;</span>)) <span class="comment"># 长度1，小端模式</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#读取数据 可以使用read readall readlines...</span></span><br><span class="line"><span class="comment">#read默认读取一个字节，可以通过传入参数指定每次读取的字节数，返回值bytes类型</span></span><br><span class="line"><span class="comment">#readall读取串口接收的全部数据，返回bytes类型</span></span><br><span class="line"><span class="comment">#readlines读取多行数据，返回list类型，列表元素类型为bytes类型</span></span><br><span class="line"><span class="built_in">print</span>(s.read(<span class="number">1024</span>))</span><br><span class="line">s.read()[<span class="number">0</span>] --》 读取一个字节</span><br><span class="line"><span class="built_in">print</span>(s.readlines())</span><br><span class="line"><span class="built_in">print</span>(s.readall())</span><br><span class="line"></span><br><span class="line"><span class="comment">#关闭串口 直接调用close方法即可</span></span><br><span class="line">s.close()</span><br><span class="line"></span><br><span class="line"><span class="comment">#其他方法</span></span><br><span class="line">in_waiting():返回接收缓存中的字节数</span><br><span class="line">flush():等待所有数据写出</span><br><span class="line">flushinput():丢弃接收缓存中的所有数据</span><br><span class="line">flushOutput():终止当前写操作，并丢弃发送缓存中的数据。</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>python_misc</category>
      </categories>
  </entry>
  <entry>
    <title>shc</title>
    <url>/shc.html</url>
    <content><![CDATA[<h1 id="shc加密脚本"><a href="#shc加密脚本" class="headerlink" title="shc加密脚本"></a>shc加密脚本</h1><p>当shell脚本中包含密码，不希望其他人查看到shell脚本中的密码等信息，可以安装使用SHC工具进行加密。</p>
<p>Shc可以用来对shell脚本进行加密，可以将shell脚本转换为一个<strong>可执行的二进制文件</strong>。经过shc对shell脚本进行加密后，会同时生成两种个新的文件，一个是加密后的可执行的二进制文件（文件名以.x结束），另一个是C语言的原文件（文件名以.x.c结束）。</p>
<h5 id="常用参数"><a href="#常用参数" class="headerlink" title="常用参数"></a>常用参数</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-e date （指定过期日期）</span><br><span class="line">-m message （指定过期提示的信息） </span><br><span class="line">-f script_name（指定要编译的shell的路径及文件名）</span><br><span class="line">-r   Relax security. （可以相同操作系统的不同系统中执行）</span><br><span class="line">-v   Verbose compilation（编译的详细情况）</span><br></pre></td></tr></table></figure>

<h5 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># shc -v -f abc.sh</span><br><span class="line">-v 是现实加密过程</span><br><span class="line">-f 后面跟需要加密的文件  </span><br><span class="line">运行后会生成两个文件: </span><br><span class="line">abc.sh.x 和 abc.sh.x.c</span><br><span class="line">abc.sh.x为二进制文件，赋予执行权限后，可直接执行。更改名字mv abc.sh.x a.sh </span><br><span class="line">abc.sh.x.c 是c源文件。基本没用，可以删除</span><br><span class="line"></span><br><span class="line">另shc还提供了一种设定有效执行期限的方法，过期时间，如：</span><br><span class="line"># shc -e 28/01/2012 -m &quot;过期了&quot; -f abc.sh</span><br><span class="line">选项“-e”指定过期时间，格式为“日/月/年”；选项“-m”指定过期后执行此shell程序的提示信息。</span><br><span class="line">如果在过期后执行，则会有如下提示：</span><br><span class="line"></span><br><span class="line"># ./abc.sh.x  </span><br><span class="line">./abc.sh.x: has expired!</span><br><span class="line">过期了</span><br></pre></td></tr></table></figure>

<h6 id="使用unshc可以解密shc加密的文件"><a href="#使用unshc可以解密shc加密的文件" class="headerlink" title="使用unshc可以解密shc加密的文件"></a>使用unshc可以解密shc加密的文件</h6>]]></content>
  </entry>
  <entry>
    <title>spi</title>
    <url>/spi.html</url>
    <content><![CDATA[<h1 id="标准spi-双线-三线-四线spi"><a href="#标准spi-双线-三线-四线spi" class="headerlink" title="标准spi,双线,三线,四线spi"></a>标准spi,双线,三线,四线spi</h1><h4 id="标准spi"><a href="#标准spi" class="headerlink" title="标准spi"></a>标准spi</h4><p>CLK,CS,MOSI,MISO,WP,Hold。在标准spi下，spi是全双工的，MOSI,MISO分别负责不同方向的传输。<strong>大多数单个SPI串行吞吐速率达到10Mbps左右。</strong></p>
<h4 id="双线spi"><a href="#双线spi" class="headerlink" title="双线spi"></a>双线spi</h4><p>CLK,CS,IO0,IO1,WP,Hold，双线spi是Dual SPI就是MOSI,MISO同时朝一个方向发数据，单词可以同时传输2bit，此时MOSI 和 MISO则被改称为IO0和IO1。在双线spi下，spi是<strong>双线半双工</strong>。串行吞吐量速率达到<strong>20Mbps</strong>左右。</p>
<h4 id="三线spi"><a href="#三线spi" class="headerlink" title="三线spi"></a>三线spi</h4><p>SCLK, MOSI,CS。数据收发只有一根线，适用于单工通讯，主机只发送或者只接收从机的数据。spi是半双工的。</p>
<h4 id="四线spi"><a href="#四线spi" class="headerlink" title="四线spi"></a>四线spi</h4><p>CLK,CS,IO0,IO1,IO2,IO3,Wp,Hold。Quad Spi，IO0,IO1,IO2,IO3被用于同一个方向传输数据。单次可传输4bit。此时MOSI和MISO为IO0,IO1,而WP,HOLD被用作IO2,IO3，CS和CLK不变。<strong>半双工40Mbps</strong></p>
]]></content>
  </entry>
  <entry>
    <title>step_motor</title>
    <url>/step-motor.html</url>
    <content><![CDATA[<ul>
<li><p><strong>步进电机工作原理本质：</strong>靠励磁绕组产生的旋转合磁场带动转子做同步运动。由于励磁绕组通电之后产生的磁通量正比于电流大小，所以只要控制通过流过各个绕组的电流大小和方向就可以控制步进电机各个绕组产生的合磁场大小和方向。</p>
</li>
<li><p><strong>相数</strong>：是指电机内部的线圈组数，如4相就是有ABCD四组线圈。</p>
</li>
<li><p><strong>拍数</strong>：是指完成一个循环的通电次数。例如按照ABCD顺序完成一个循环，就称为单4拍。相邻的两个线圈也可以同时通电，例如可以按照AB-BC-CD-DA方式通电，这种就称为双4拍。注意，对同一个电机来说，单四拍与双四拍每拍转动的角度是相同的。还有一种方式是单个线圈与双个线圈轮流通电，就是A-AB-B-BC-C-CD-D-DA，这样就是四相八拍，这种方式工作时每拍转动的角度是4拍的一半。</p>
</li>
<li><p><strong>励磁方式</strong>：分为全步励磁和半步励磁，其中全步励磁又有一相励磁（在没每一瞬间步进电机只有一个线圈导通，步进电机旋转1.8度）和二相励磁（在每一瞬间，步进电机有两个线圈同时导通1.8度）；半步励磁又称一二相励磁（线圈交替导通）。</p>
</li>
<li><p><strong>步距角</strong>：对应一个脉冲信号，电机转子转过的角位移用θ表示。θ&#x3D;360度（转子齿数J*运行拍数），以常规二、四相，转子齿为50齿电机为例。四拍运行时步距角为θ&#x3D;360度&#x2F;（50*4）&#x3D;1.8度（俗称整步），八拍运行时步距角为θ&#x3D;360度&#x2F;（50*8）&#x3D;0.9度（俗称半步）。</p>
</li>
<li><p><strong>细分：</strong>通过等角度有规律的插入大小相等的电流合成向量，从而减小合成磁势的步距角。力矩越大，步进角越大。正弦细分驱动的本质实际上就是通过绕组A和绕组B的电流分别按照正，余弦规律变化，使得合成电流矢量圆周均匀旋转。</p>
</li>
<li><p><strong>升降速控制：</strong>运动速度根据运动参数当中的细分数和步数做选择。细分数约大平均运动速度越慢，反之越快。比如细分1024，相同的步数速度则会很慢。</p>
</li>
<li><p><strong>力矩速率关系：</strong>当步进电机转动时，电机各相绕组的电感将形成一个反向电动势；频率越高，反向电动势越大。在它的作用下，电机随频率（或速度）的增大而相电流减小，从而导致力矩下降。</p>
</li>
<li><p><strong>空载启动频率</strong>：即步进电机在空载情况下能够正常启动的脉冲频率，如果脉冲频率高于该值，电机不能正常启动，可能发生丢步或堵转。在有负载的情况下，启动频率应更低。如果要使电机达到高速转动，脉冲频率应该有加速过程，即启动频率较低，然后按一定加速度升到所希望的高频（电机转速从低速升到高速）。</p>
</li>
<li><p><strong>合成电流：</strong>以两相电机为例，如果控制绕组的电流按照正余弦变化，那么绕组合成电流矢量或者磁场矢量将以恒定大小，均匀角度做圆周运动。使得力矩恒定，步距角均匀。</p>
<p><img src="/../image/image-20240328203237016.png" alt="image-20240328203237016"></p>
</li>
</ul>
<h3 id="SPWM-脉冲宽度调制技术"><a href="#SPWM-脉冲宽度调制技术" class="headerlink" title="SPWM 脉冲宽度调制技术"></a>SPWM 脉冲宽度调制技术</h3><h4 id="PWM"><a href="#PWM" class="headerlink" title="PWM"></a>PWM</h4><p><u>目前一般采用脉冲宽度调制（PWM）技术来精确控制绕组电流的大小。主要是因为冲量相等而形状不同的窄脉冲加在具有惯性的环 节上,效果基本相同。步进电机的绕组由于电流不会突变，具有明显的惯性 环节,因此可以用 PWM 技术来控制电机绕组的电流大小。</u></p>
<p>如图所示。周期脉冲 信号的导通阶段对绕组进行充电，截止阶段绕组通过续流回路 进行放电。当脉冲的频率和宽度达到一定值时，绕组的电流将基本是一个恒定值， 并带有微小的纹波信号。当脉冲宽度改变时，绕组的电流也将发生变化。所以 PWM 可用来精确控制绕组电流的大小。</p>
<p><img src="/../image/image-20240328203617119.png" alt="image-20240328203617119"></p>
<p>即pulse width modulation，脉冲宽度调制，实际上就是周期的矩形波，然后每个周期的占空比都可以自己设置就叫做调制。</p>
<p>相比SPWM，多加了一个S，即sin，正弦脉宽调制。还是这个周期的矩形波，但不同的是占空比不是固定的，而是按照正弦规律变化的。</p>
<p>SPWM一般由三角波（载波）和正弦波（调制波）比较而成，硬件生成方法是将三角波和正弦波加入比较器得到，软件是通过定时器或Epwm模块，按照中央计数模式生成三角波，经CCR比较模块动作产生对于高电平，即SPWM。</p>
<h4 id="升降频控制技术"><a href="#升降频控制技术" class="headerlink" title="升降频控制技术"></a>升降频控制技术</h4><p>步进电机从静止启动时，由于惯性和摩擦力矩的作用，如果转动频率突变太 大可能会丢步甚至堵转；当步进电机在高速运转时如果突然停下来，则可能会过 冲，这些情况都会导致运动不平稳以及定位精度不高。所以引出了升降频控制技术。</p>
<p>由于步进电机升速过程当中输出力矩明显减少，因而步进电机的升速曲线的 设计尤为重要。步进电机的升速过程一般由突变频率和加速曲线过程。</p>
<p>实际上步进电机转动频率不是连续变化的而是离散的，因而升速曲线一般 是指运行频率与脉冲数的关系曲线。由于步进电机<u>降速过程中输出力矩增大，因而对降速曲线的要求比升速曲线 低得多，只要保证不因为惯性而过冲超步即可</u>。</p>
<p><img src="/../image/image-20240328204102555.png" alt="image-20240328204102555"></p>
<h4 id="SPWM-软件生成"><a href="#SPWM-软件生成" class="headerlink" title="SPWM 软件生成"></a>SPWM 软件生成</h4><p>利用单片机来生成PWM，然后让占空比按照正弦规律变化。</p>
<h6 id="步骤"><a href="#步骤" class="headerlink" title="步骤"></a>步骤</h6><ul>
<li><p>生成载波，比如要生成一个10KHZ的三角波，将计数器设置加减计数、周期设为1&#x2F;10K</p>
</li>
<li><p>生成正弦波，用软件生成正弦表即可，</p>
</li>
<li><p>将正弦波和三角波比较。设置<strong>计数值到达</strong>的时候进行比较，改变<strong>比较值</strong>，用查表法获得，用于下一个周期比较。<strong>调制度m</strong> &#x3D; 正弦表最大值&#x2F;三角波计数最大值。 如正弦表最大值4200，三角波最大计数值8400，m&#x3D;4200&#x2F;8400&#x3D;0.5，此时spwm最大占空比为50%，设置m&#x3D;1，spwm最大占空比为100%。</p>
<p><img src="/../image/image-20240325084615541.png" alt="image-20240325084615541"></p>
<p>要注意因为是单极性调制，spwm和三角载波都是大于0的。在单相全桥逆变电路中，开关管交替导通时输出电压Ud自然会倒过来为负，Ud经过滤波就是一个正弦波。</p>
</li>
</ul>
<h4 id="定时器相关"><a href="#定时器相关" class="headerlink" title="定时器相关"></a>定时器相关</h4><p>一般电机控制都是边沿对齐模式，FOC电机一般使用中心对齐模式。</p>
<p>边沿对齐模式 –&gt; 在递增计数模式下，计数器从0递增到ARR值，然后重新从0开始计数并产生计数器上溢事件。</p>
<h4 id="DSP控制三相步进电机时，为什么会用一对互补的PWM波形来控制？"><a href="#DSP控制三相步进电机时，为什么会用一对互补的PWM波形来控制？" class="headerlink" title="DSP控制三相步进电机时，为什么会用一对互补的PWM波形来控制？"></a>DSP控制三相步进电机时，为什么会用一对互补的PWM波形来控制？</h4><p>对于某种特定的电机来说，每一相位都有高位的MOS和低位的MOS，要驱动这两个MOS还不能同时开和同时关，必须上开下关或者下开上关，所以要用互补的PWM来控制。如果是用专门的MOS驱动芯片驱动的话，是可以用一路PWM的。</p>
]]></content>
      <categories>
        <category>miscg</category>
      </categories>
  </entry>
  <entry>
    <title>syslog</title>
    <url>/syslog.html</url>
    <content><![CDATA[<h5 id="syslog-conf介绍"><a href="#syslog-conf介绍" class="headerlink" title="syslog.conf介绍"></a>syslog.conf介绍</h5><p>syslog采用可配置的、统一的系统登记程序，随时从系统各处接受log请求，然后根据**&#x2F;etc&#x2F;syslog.conf**中的预先设定把log信息写入相应文件中、邮寄给特 定用户或者直接以消息的方式发往控制台。ps：不同平台下的文件不一定相同，可以在etc下grep &#x2F;var&#x2F;log&#x2F;messages等来查看文件名。</p>
<h5 id="具体示例"><a href="#具体示例" class="headerlink" title="具体示例"></a>具体示例</h5><p>*.err;kern.debug;daemon.notice;mail.crit [TAB] &#x2F;var&#x2F;adm&#x2F;messages</p>
<p>这行中的“action”就是我们常关心的那个&#x2F;var&#x2F;adm&#x2F;messages文件，输出到它的信息源头“selector”是：</p>
<p>*.err – 所有的一般错误信息；</p>
<p>kern.debug – 核心产生的调试信息；</p>
<p>daemon.notice – 守护进程的注意信息；</p>
<p>mail.crit – 邮件系统的关键警告信息</p>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
  </entry>
  <entry>
    <title>test_1</title>
    <url>/test-1.html</url>
    <content><![CDATA[<h3 id="原码补码反码移码"><a href="#原码补码反码移码" class="headerlink" title="原码补码反码移码"></a>原码补码反码移码</h3><p><strong>反码</strong>：解决负数加法运算问题，将减法运算转换为加法运算，从而简化运算规则；<br>        <strong>补码</strong>：解决负数加法运算正负零问题，弥补了反码的不足。</p>
<p>总之，反码与补码都是为了解决负数运算问题，跟正数没关系，因此，<strong>不管是正整数还是正小数，原码，反码，补码都全部相同。</strong></p>
<p><strong>总结：</strong></p>
<ul>
<li>正数的原码、补码、反码均为其本身；</li>
<li>负数（二进制）的原码、补码、反码公式：<br>反码 &#x3D; 原码（除符号位外）每位取反<br>补码 &#x3D; 反码 + 1<br>反码 &#x3D; 补码  - 1<br>移码 &#x3D; 补码符号位取反</li>
</ul>
<h3 id="计算机可靠性模型"><a href="#计算机可靠性模型" class="headerlink" title="计算机可靠性模型"></a>计算机可靠性模型</h3><h5 id="串联系统"><a href="#串联系统" class="headerlink" title="串联系统"></a>串联系统</h5><p>系统可靠性<img src="/../image/image-20240418103809734.png" alt="image-20240418103809734"></p>
<p>系统失效率<img src="/../image/image-20240418103855822.png" alt="image-20240418103855822"></p>
<h5 id="并联系统"><a href="#并联系统" class="headerlink" title="并联系统"></a>并联系统</h5><p>系统可靠性<img src="/../image/image-20240418103948295.png" alt="image-20240418103948295"></p>
<p>系统失效率<img src="/../image/image-20240418104000114.png" alt="image-20240418104000114"></p>
]]></content>
      <categories>
        <category>software_test</category>
      </categories>
  </entry>
  <entry>
    <title>uart-speed</title>
    <url>/uart-speed.html</url>
    <content><![CDATA[<h4 id="串口波特率计算补充"><a href="#串口波特率计算补充" class="headerlink" title="串口波特率计算补充"></a>串口波特率计算补充</h4><p>由于单片机内部的RC时钟引起的误差而导致的产品失效，比如 Uart串口通信(常温不可以通信，温度高才行）、Watchdog看门狗复位失灵等等。所以在这里补充一下串口波特率相关计算。“串口波特率的最大偏差多少，对方就不能接收了或出错了？”</p>
<p>最常见的10位串口的输出时序( 1个起始位 + 8个数据位  + 1个停止位 ）</p>
<p><img src="/../image/image-20240401172627745.png" alt="image-20240401172627745"></p>
<p>我们要确保实际波特率对应的 D0 ~ D7 时区段，都分别落在对方波特率的采样点的范围内，</p>
<p><strong>从多个常见的波特率推送来看，****最大偏差一般在5% 以内都可以正常通信。</strong></p>
<p>但考虑到不同芯片的采样点的方式有所不同，以及一帧的串口位不同(数据位越长，累计的偏差会更多），建议确保波特率控制在2%以内最好。</p>
<p><img src="/../image/image-20240401174220374.png" alt="image-20240401174220374"></p>
<ul>
<li>值得注意的是，每次接收数据采样点均会从第1 bit 数据初始化，这意味着采样偏差并不会累积至下一次数据传输，如图1所示，每次检测到下降沿时将重新开始进行数据采样。</li>
</ul>
<p><img src="/../image/image-20240401174311722.png" alt="image-20240401174311722"></p>
]]></content>
      <categories>
        <category>mcu_misc</category>
      </categories>
  </entry>
  <entry>
    <title>usb和pcie总线对比</title>
    <url>/usb_pcie_bus.html</url>
    <content><![CDATA[<h5 id="几种类型usb控制器"><a href="#几种类型usb控制器" class="headerlink" title="几种类型usb控制器"></a>几种类型usb控制器</h5><h6 id="OHCI-Open-Host-Controller-Interface"><a href="#OHCI-Open-Host-Controller-Interface" class="headerlink" title="OHCI(Open Host Controller Interface)"></a>OHCI(Open Host Controller Interface)</h6><p>是支持USB1.1的标准，它不仅是针对USB，还支持一些其它接口，比如Apple的火线接口，与UHCI相比，OHCI的硬件复杂，硬件做的事情更多，所以实现对应的软件驱动的任务，就相对比较简单，主要用于非X86的USB。</p>
<h6 id="UHCI-Universal-Host-Controller-Interface"><a href="#UHCI-Universal-Host-Controller-Interface" class="headerlink" title="UHCI(Universal Host Controller Interface)"></a>UHCI(Universal Host Controller Interface)</h6><p>是Intel主导的对USB1.0，1.1的接口标准，与OHCI不兼容，UHCI的软件驱动任务重，需要做的比较复杂，但可以使用较便宜的USB控制器，Inter和VIA使用UHCI，其余硬件提供商使用OHCI</p>
<h6 id="EHCI-Enhanced-Host-Controller-Interface"><a href="#EHCI-Enhanced-Host-Controller-Interface" class="headerlink" title="EHCI(Enhanced Host Controller Interface)"></a>EHCI(Enhanced Host Controller Interface)</h6><p>是Intel主导的对USB2.0的接口标准，仅提供USB2.0的高速功能。</p>
<h6 id="XHCI-eXtensible-Host-Controller-Interface"><a href="#XHCI-eXtensible-Host-Controller-Interface" class="headerlink" title="XHCI(eXtensible Host Controller Interface)"></a>XHCI(eXtensible Host Controller Interface)</h6><p>是最新最火的USB3.0的接口标准，它在速度、节能、虚拟化等方面都比前面3中有了较大的提高。xHCI 支持所有种类速度的USB设备（USB 3.0 SuperSpeed, USB 2.0 Low-, Full-, and High-speed, USB 1.1 Low- and Full-speed）。xHCI的目的是为了替换前面3中（UHCI&#x2F;OHCI&#x2F;EHCI）。</p>
<blockquote>
<p>AHCI (Serial ATA Advanced Host Controller Interface)，串行ATA高级主控接口，允许存储驱动程序启用高级串行ATA功能。AHCI通过包含一个PCI BAR（基址寄存器），来实现原生SATA功能。（ATA是存储设备的标准，AHCI是控制器的接口）</p>
</blockquote>
<h5 id="PCI-和-PCIE-比较"><a href="#PCI-和-PCIE-比较" class="headerlink" title="PCI 和 PCIE 比较"></a>PCI 和 PCIE 比较</h5><p>PCI将较于PCIE有两个大的区别。</p>
<ul>
<li>PCI的数据是并行的，有32bit数据地址线，典型时钟133MHz，而PCIE的数据线是串行的，没有单独的时钟线（注意，100M参考时钟是给pcie控制器使用的），这样速率可以上GHz。</li>
<li>PCI是多点共联，通俗讲就是各个器件在物理上共同占用数据线，就会造成时钟频率上不去，延时增加等等，还需要考虑负载因素。而PCIE是point to point，一个口和一个口直接对接，所以负载小，频率高。如果很多设备想像PCI一样挂在一起给CPU用，加入switch即可。</li>
</ul>
<h5 id="PCIE-USB"><a href="#PCIE-USB" class="headerlink" title="PCIE &amp;&amp; USB"></a>PCIE &amp;&amp; USB</h5><p>PCIe，USB，以太网。都是串行总线。传输距离逐渐变远。<strong>PCIe 整个组网里网络报文的流动靠的是硬件驱动的</strong>。软件不参与报文的驱动流动。USB，需要软件来参与报文的驱动和流动。所以<strong>USB整个组网里还包括了软件部分</strong>。也就是USB软件协议栈。以太网 整个组网里也是，需要软件协议栈参与以太网组网里的报文流动。</p>
<p>PCIE是和USB都是高速总线，拓扑都是类似的树形结构，都是以CPU为ROOT，系统中外挂其他的外设作为树的终端节点，给CPU控制和使用，且二者在物理层，数据链路层，速率等各个方面都高度相似。但是USB的作用仅仅是数据的传输的数据线，PCIE想做的事情远远复杂于USB。</p>
<p>以arm体系架构为例， CPU之外的外设，想要干些什么？除了被CPU访问控制寄存器（被动IO访问），被CPU或DMA访问SRAM（被动MEM访问）之外，还希望能访问系统内存（主动MEM访问），上报中断（中断消息）,流量控制和仲裁（多外设数据流的流量控制），cache一致性（cache等功能）。</p>
<p>可以看到，功能很多，方向也是多向的，不仅仅是CPU到设备，也可能是设备到DDR及其他设备。这一切PCIE都做进去了，比如你的设备可以通过message包上报中断，PCIE的RC会把中断以物理连线的方式给arm的GIC中断控制器。</p>
<p>反观USB呢？他的核心仅仅是数据传输，比如BULK transfer是U盘这种大数据传输；INTR transfer是给鼠标这种小数据传输；ISO transfer是给摄像头这种等时传输。数据的搬运通道而已，并不涉及计算体系架构。</p>
]]></content>
      <categories>
        <category>pcie</category>
      </categories>
  </entry>
</search>
